import{c as s,Q as e,j as t,m as n}from"./chunks/framework.DOi4mjdC.js";const l="/assets/%E6%A0%B8%E5%87%BD%E6%95%B0-host%E4%BE%A7%E6%89%A7%E8%A1%8C%E5%87%BD%E6%95%B0-device%E4%BE%A7%E6%89%A7%E8%A1%8C%E5%87%BD%E6%95%B0%E8%B0%83%E7%94%A8%E5%85%B3%E7%B3%BB.DZlGIUJa.png",m=JSON.parse('{"title":"核函数","description":"","frontmatter":{},"headers":[{"level":1,"title":"核函数","slug":"核函数"},{"level":2,"title":"核函数定义和调用","slug":"核函数定义和调用"},{"level":2,"title":"模板核函数定义和调用","slug":"模板核函数定义和调用"}],"relativePath":"guide/编程指南/编程模型/AI-Core-SIMD编程/核函数.md","filePath":"guide/编程指南/编程模型/AI-Core-SIMD编程/核函数.md"}'),p={name:"guide/编程指南/编程模型/AI-Core-SIMD编程/核函数.md"};function i(o,a,c,r,d,_){return e(),t("div",null,[...a[0]||(a[0]=[n(`<h1 id="核函数" tabindex="-1">核函数 <a class="header-anchor" href="#核函数" aria-label="Permalink to &quot;核函数&quot;">​</a></h1><p>核函数（Kernel Function）是Ascend C算子设备侧实现的入口。Ascend C允许用户使用C/C++函数的语法扩展来编写设备端的运行代码，用户在核函数中进行数据访问和计算操作，由此实现该算子的所有功能。区别于普通的C++函数调用时仅执行一次，当核函数被调用时，多个核都执行相同的核函数代码，具有相同的函数入参，并行执行。</p><p>核函数定义时需要使用函数类型限定符__global__和__aicore__；其指针入参变量需要增加变量类型限定符__gm__，表明该指针变量指向Global Memory上某处内存地址；使用&lt;&lt;&lt;...&gt;&gt;&gt;内核调用符调用执行核函数，并指定调用时的执行核数。</p><p>以下是一个Add算子的核函数示例（代码片段）。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>// 实现核函数</span></span>
<span class="line"><span>__global__ __aicore__ void add_custom(__gm__ uint8_t* x, __gm__ uint8_t* y, __gm__ uint8_t* z)</span></span>
<span class="line"><span>{</span></span>
<span class="line"><span>    // 初始化算子类，算子类提供算子初始化和核心处理等方法</span></span>
<span class="line"><span>    KernelAdd op;</span></span>
<span class="line"><span>    // 初始化函数，获取该核函数需要处理的输入输出地址，同时完成必要的内存初始化工作</span></span>
<span class="line"><span>    op.Init(x, y, z);</span></span>
<span class="line"><span>    // 核心处理函数，完成算子的数据搬运与计算等核心逻辑</span></span>
<span class="line"><span>    op.Process();</span></span>
<span class="line"><span>}</span></span>
<span class="line"><span></span></span>
<span class="line"><span>// 调用核函数</span></span>
<span class="line"><span>void add_custom_do(uint32_t numBlocks, void* l2ctrl, void* stream, uint8_t* x, uint8_t* y, uint8_t* z)</span></span>
<span class="line"><span>{</span></span>
<span class="line"><span>    add_custom&lt;&lt;&lt;numBlocks, l2ctrl, stream&gt;&gt;&gt;(x, y, z);</span></span>
<span class="line"><span>}</span></span></code></pre></div><h2 id="核函数定义和调用" tabindex="-1">核函数定义和调用 <a class="header-anchor" href="#核函数定义和调用" aria-label="Permalink to &quot;核函数定义和调用&quot;">​</a></h2><p>定义核函数时需要遵循以下规则。</p><ul><li><p><strong>使用函数类型限定符</strong></p><p>除了需要按照C/C++函数声明的方式定义核函数之外，还要为核函数加上额外的函数类型限定符，包含__global__和__aicore__。</p><p>使用__global__函数类型限定符来标识它是一个核函数，可以被&lt;&lt;&lt;...&gt;&gt;&gt;调用；使用__aicore__函数类型限定符来标识该核函数在设备端AI Core上执行：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>__global__ __aicore__ void kernel_name(argument list);</span></span></code></pre></div><p>编程中使用到的函数可以分为三类：核函数（device侧执行）、host侧执行函数、device侧执行函数（除核函数之外）。下图以Kernel直调算子开发方式为例描述三者的调用关系：</p><ul><li>host侧执行函数可以调用同类的host执行函数，也就是通用C/C++编程中的函数调用；也可以通过&lt;&lt;&lt;...&gt;&gt;&gt;调用核函数。</li><li>device侧执行函数（除核函数之外）可以调用同类的device侧执行函数。</li><li>核函数可以调用device侧执行函数（除核函数之外）。</li></ul><p><strong>图 1</strong> 核函数、host侧执行函数、device侧执行函数调用关系<br><img src="`+l+`" alt="" title="核函数-host侧执行函数-device侧执行函数调用关系"></p></li><li><p><strong>使用变量类型限定符</strong></p><p>指针入参变量需要增加变量类型限定符__gm__，表明该指针变量指向Global Memory上某处内存地址。</p></li><li><p><strong>其他规则或建议</strong></p><ol><li><p>规则：核函数必须具有void返回类型。</p></li><li><p>规则：仅支持入参为指针或C/C++内置数据类型（Primitive data types），如：half* s0、float* s1、int32_t c。</p></li><li><p>建议：为了统一表达，建议使用GM_ADDR宏来修饰入参，GM_ADDR宏定义如下：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#define GM_ADDR __gm__ uint8_t*</span></span></code></pre></div><p>使用GM_ADDR修饰入参的样例如下：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>extern &quot;C&quot; __global__ __aicore__ void add_custom(GM_ADDR x, GM_ADDR y, GM_ADDR z)</span></span></code></pre></div><p>这里统一使用uint8_t类型的指针，在后续的使用中需要将其转化为实际的指针类型。</p></li></ol></li></ul><p>常见的函数调用方式是如下的形式：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>function_name(argument list);</span></span></code></pre></div><p>核函数使用内核调用符&lt;&lt;&lt;...&gt;&gt;&gt;这种语法形式，来规定核函数的执行配置：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>kernel_name&lt;&lt;&lt;numBlocks, l2ctrl, stream&gt;&gt;&gt;(argument list);</span></span></code></pre></div><p>内核调用符仅可在NPU侧编译时调用，CPU侧编译无法识别该符号。</p><p>执行配置由3个参数决定：</p><ul><li><p>numBlocks，规定了核函数将会在几个核上执行。每个执行该核函数的核会被分配一个逻辑ID，即block_idx，可以在核函数的实现中调用<a href="https://gitcode.com/cann/asc-devkit/blob/master/docs/api/context/GetBlockIdx.md" target="_blank" rel="noreferrer">GetBlockIdx</a>来获取block_idx；</p><div class="note custom-block github-alert"><p class="custom-block-title">说明</p><p>numBlocks是逻辑核的概念，取值范围为[1,65535]。为了充分利用硬件资源，一般设置为物理核的核数或其倍数。</p><ul><li>对于耦合模式和分离模式，numBlocks在运行时的意义和设置规则有一些区别，具体说明如下： <ul><li>耦合模式：由于其Vector、Cube单元是集成在一起的，numBlocks用于设置启动多个AI Core核实例执行，不区分Vector、Cube。AI Core的核数可以通过<a href="https://gitcode.com/cann/asc-devkit/blob/master/docs/api/context/GetCoreNumAiv.md" target="_blank" rel="noreferrer">GetCoreNumAiv</a>或者<a href="https://gitcode.com/cann/asc-devkit/blob/master/docs/api/context/GetCoreNumAic.md" target="_blank" rel="noreferrer">GetCoreNumAic</a>获取。</li><li>分离模式 <ul><li>针对仅包含Vector计算的算子，numBlocks用于设置启动多少个Vector（AIV）实例执行，比如某款AI处理器上有40个Vector核，建议设置为40。</li><li>针对仅包含Cube计算的算子，numBlocks用于设置启动多少个Cube（AIC）实例执行，比如某款AI处理器上有20个Cube核，建议设置为20。</li><li>针对Vector/Cube融合计算的算子，启动时，按照AIV和AIC组合启动，numBlocks用于设置启动多少个组合执行，比如某款AI处理器上有40个Vector核和20个Cube核，一个组合是2个Vector核和1个Cube核，建议设置为20，此时会启动20个组合，即40个Vector核和20个Cube核。<strong>注意：该场景下，设置的numBlocks逻辑核的核数不能超过物理核（2个Vector核和1个Cube核组合为1个物理核）的核数。</strong></li><li>AIC/AIV的核数分别通过<a href="https://gitcode.com/cann/asc-devkit/blob/master/docs/api/context/GetCoreNumAic.md" target="_blank" rel="noreferrer">GetCoreNumAic</a>和<a href="https://gitcode.com/cann/asc-devkit/blob/master/docs/api/context/GetCoreNumAiv.md" target="_blank" rel="noreferrer">GetCoreNumAiv</a>接口获取。</li></ul></li></ul></li><li>如果开发者使用了Device资源限制特性，那么算子设置的numBlocks不应超过<a href="https://gitcode.com/cann/asc-devkit/blob/master/docs/api/context/PlatformAscendC.md" target="_blank" rel="noreferrer">PlatformAscendC</a>提供核数的API（GetCoreNum/GetCoreNumAic/GetCoreNumAiv等）返回的核数。例如，使用aclrtSetStreamResLimit设置Stream级别的Vector核数为8，那么GetCoreNumAiv接口返回值为8，针对Vector算子设置的numBlocks不应超过8，否则会抢占其他Stream的资源，导致资源限制失效。</li></ul></div></li><li><p>l2ctrl，保留参数，暂时设置为固定值nullptr，开发者无需关注；</p></li><li><p>stream，类型为aclrtStream，stream用于维护一些异步操作的执行顺序，确保按照应用程序中的代码调用顺序在device上执行。stream创建等管理接口请参考<a href="https://hiascend.com/document/redirect/CannCommunityRuntimeApi" target="_blank" rel="noreferrer">《Runtime运行时API》</a>。</p></li></ul><p>如下名为add_custom的核函数，实现两个矢量的相加，调用示例如下：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>// numBlocks设置为8表示在8个核上调用了add_custom核函数，每个核都会独立且并行地执行该核函数，该核函数的参数列表为x，y，z。</span></span>
<span class="line"><span>add_custom&lt;&lt;&lt;8, nullptr, stream&gt;&gt;&gt;(x, y, z);</span></span></code></pre></div><p>核函数的调用是异步的，核函数的调用结束后，控制权立刻返回给主机端，可以调用以下<strong>aclrtSynchronizeStream</strong>函数来强制主机端程序等待所有核函数执行完毕。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>aclError aclrtSynchronizeStream(aclrtStream stream);</span></span></code></pre></div><p>aclrtSynchronizeStream的具体用法参考<a href="https://hiascend.com/document/redirect/CannCommunityRuntimeApi" target="_blank" rel="noreferrer">《Runtime运行时API》</a>。</p><h2 id="模板核函数定义和调用" tabindex="-1">模板核函数定义和调用 <a class="header-anchor" href="#模板核函数定义和调用" aria-label="Permalink to &quot;模板核函数定义和调用&quot;">​</a></h2><p>支持开发者使用模板定义核函数，核函数定义示例如下，它有两个模板参数：a和T。a是一个非类型模板参数，T是一个类型模板参数。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>template&lt;int a, typename T&gt;</span></span>
<span class="line"><span>__global__ __aicore__ void add_custom(GM_ADDR x, GM_ADDR y, GM_ADDR z)</span></span>
<span class="line"><span>{</span></span>
<span class="line"><span>...</span></span>
<span class="line"><span>    AscendC::printf(&quot;Print Template a: %d\\n&quot;, a);</span></span>
<span class="line"><span>...</span></span>
<span class="line"><span>    xGm.SetGlobalBuffer((__gm__T*)x + BLOCK_LENGTH * AscendC::GetBlockIdx(), BLOCK_LENGTH);</span></span>
<span class="line"><span>    yGm.SetGlobalBuffer((__gm__T*)y + BLOCK_LENGTH * AscendC::GetBlockIdx(), BLOCK_LENGTH);</span></span>
<span class="line"><span>    zGm.SetGlobalBuffer((__gm__T*)z + BLOCK_LENGTH * AscendC::GetBlockIdx(), BLOCK_LENGTH);</span></span>
<span class="line"><span>...</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>模板核函数的调用方式如下：add_custom&lt;20, float&gt;这部分代码调用了名为add_custom的核函数，并为其模板参数提供了具体值。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>add_custom&lt;20, float&gt;&lt;&lt;&lt;numBlocks, nullptr, stream&gt;&gt;&gt;(x, y, z);</span></span></code></pre></div>`,25)])])}const g=s(p,[["render",i]]);export{m as __pageData,g as default};
