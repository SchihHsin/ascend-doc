import{c as s,Q as n,j as e,m as p}from"./chunks/framework.DOi4mjdC.js";const l="/assets/%E5%8D%95%E7%AE%97%E5%AD%90API%E6%89%A7%E8%A1%8C%E6%8E%A5%E5%8F%A3%E8%B0%83%E7%94%A8%E6%B5%81%E7%A8%8B.DbyBRmyA.png",_=JSON.parse('{"title":"单算子API调用","description":"","frontmatter":{},"headers":[{"level":1,"title":"单算子API调用","slug":"单算子api调用"},{"level":2,"title":"基本原理","slug":"基本原理"},{"level":2,"title":"前置步骤","slug":"前置步骤"},{"level":2,"title":"准备验证代码工程","slug":"准备验证代码工程"},{"level":2,"title":"单算子调用流程","slug":"单算子调用流程"},{"level":2,"title":"CMakeLists文件","slug":"cmakelists文件"},{"level":2,"title":"编译与运行","slug":"编译与运行"}],"relativePath":"guide/编程指南/高级编程/高级特性/Aclnn算子工程化开发/单算子API调用.md","filePath":"guide/编程指南/高级编程/高级特性/Aclnn算子工程化开发/单算子API调用.md"}'),t={name:"guide/编程指南/高级编程/高级特性/Aclnn算子工程化开发/单算子API调用.md"};function i(c,a,o,r,d,u){return n(),e("div",null,[...a[0]||(a[0]=[p(`<h1 id="单算子api调用" tabindex="-1">单算子API调用 <a class="header-anchor" href="#单算子api调用" aria-label="Permalink to &quot;单算子API调用&quot;">​</a></h1><p>单算子API调用方式，是指直接调用单算子API接口，基于C语言的API执行算子。算子工程创建完成后，基于工程代码框架完成算子原型定义、kernel侧算子实现、host侧tiling实现，通过工程编译脚本完成算子的编译部署，之后再进行单算子API的调用。</p><h2 id="基本原理" tabindex="-1">基本原理 <a class="header-anchor" href="#基本原理" aria-label="Permalink to &quot;基本原理&quot;">​</a></h2><p>完成自定义算子编译后，会自动生成单算子API，可以直接在应用程序中调用。</p><p>单算子API的形式一般定义为“两段式接口”，形如：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>aclnnStatus aclnnXxxGetWorkspaceSize(const aclTensor *src, ..., aclTensor *out, uint64_t *workspaceSize, aclOpExecutor **executor);</span></span>
<span class="line"><span>aclnnStatus aclnnXxx(void *workspace, uint64_t workspaceSize, aclOpExecutor *executor, aclrtStream stream);</span></span></code></pre></div><p>其中aclnn_Xxx_GetWorkspaceSize/aclnn_Xxx_TensorGetWorkspaceSize为第一段接口，主要用于计算本次API调用过程中需要多少workspace内存，获取到本次计算所需的workspaceSize后，按照workspaceSize申请NPU内存，然后调用第二段接口aclnn_Xxx_执行计算_。Xxx代表<a href="./算子原型定义.html">算子原型注册</a>时传入的算子类型。</p><p><strong>aclnn_Xxx_GetWorkspaceSize</strong>接口的输入输出参数生成规则如下：</p><ul><li><p>可选输入的命名增加Optional后缀。如下样例中，x是可选输入。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>aclnnStatus aclnnXxxGetWorkspaceSize(const aclTensor *xOptional, ..., aclTensor *out, uint64_t *workspaceSize, aclOpExecutor **executor);</span></span></code></pre></div></li><li><p>输入输出同名、使用同一个Tensor承载的情况下，生成的aclnn接口中只保留input参数同时去掉input的const修饰，并以Ref作为后缀。如下样例中，原型定义input、output都定义为x，xRef既作为输入，又作为输出。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>aclnnStatus aclnnXxxGetWorkspaceSize(aclTensor *xRef, ..., uint64_t *workspaceSize, aclOpExecutor **executor);</span></span></code></pre></div></li><li><p>如果仅有一个输出，输出参数命名为out；如果存在多个输出，每个输出后面都以Out作为后缀。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>// 仅有一个输出</span></span>
<span class="line"><span>aclnnStatus aclnnXxxGetWorkspaceSize(const aclTensor *src, ..., aclTensor *out, uint64_t *workspaceSize, aclOpExecutor **executor);</span></span>
<span class="line"><span>// 存在多个输出</span></span>
<span class="line"><span>aclnnStatus aclnnXxxGetWorkspaceSize(const aclTensor *src, ..., aclTensor *yOut, aclTensor *y1Out, ..., uint64_t *workspaceSize, aclOpExecutor **executor);</span></span></code></pre></div></li><li><p>如果算子包含属性，则属性参数的位置介于输入输出之间。如下示例中，x是算子输入，negativeSlope是算子属性，out是算子输出。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>aclnnStatus aclnnXxxGetWorkspaceSize(const aclTensor *x, double negativeSlope, aclTensor *out, uint64_t *workspaceSize, aclOpExecutor **executor);</span></span></code></pre></div></li></ul><p>当算子原型注册时使用ValueDepend接口标识输入为数据依赖输入时，会额外生成一个API，该API支持值依赖场景输入数据为空的一阶段计算。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>aclnnStatus aclnnXxxTensorGetWorkspaceSize(const aclTensor *src, ..., aclTensor *out, uint64_t *workspaceSize, aclOpExecutor **executor);</span></span></code></pre></div><p><strong>在aclnn_Xxx_TensorGetWorkspaceSize</strong>中，<strong>aclnn_Xxx_GetWorkspaceSize</strong>参数的数据类型（aclIntArray、aclFloatArray和aclBoolArray）将被转换为aclTensor数据类型，其他输入输出参数生成规则与<strong>aclnn_Xxx_GetWorkspaceSize</strong>一致。如下示例中，x0、x1、x2是算子声明为数据依赖的输入，数据类型分别为DT_INT64、DT_BOOL、DT_FLOAT，out是算子输出。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>aclnnStatus aclnnXxxGetWorkspaceSize(const aclIntArray *x0, const aclBoolArray *x1, const aclFloatArray *x2, aclTensor *out, uint64_t *workspaceSize, aclOpExecutor **executor);</span></span>
<span class="line"><span>aclnnStatus aclnnXxxTensorGetWorkspaceSize(const aclTensor *x0, const aclTensor *x1, const aclTensor *x2, aclTensor *out, uint64_t *workspaceSize, aclOpExecutor **executor);</span></span></code></pre></div><h2 id="前置步骤" tabindex="-1">前置步骤 <a class="header-anchor" href="#前置步骤" aria-label="Permalink to &quot;前置步骤&quot;">​</a></h2><ul><li><p>参考<a href="./创建算子工程.html">创建算子工程</a>完成自定义算子工程的创建。</p></li><li><p>参考<a href="./Kernel侧算子实现.html">Kernel侧算子实现</a>完成kernel侧实现的相关准备，参考<a href="./Host侧Tiling实现/基本流程.html">Host侧Tiling实现</a>、<a href="./算子原型定义.html">算子原型定义</a>完成host侧实现相关准备。</p></li><li><p>对于算子包编译场景，参考<a href="./算子包编译/算子工程编译.html">算子工程编译</a>、<a href="./算子包编译/算子包部署.html">算子包部署</a>完成算子的编译部署，编译部署时需要开启算子的二进制编译功能：修改算子工程中的编译配置项文件CMakePresets.json，将ENABLE_BINARY_PACKAGE设置为True。编译部署时可将算子的二进制部署到当前环境，便于后续算子的调用。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&quot;ENABLE_BINARY_PACKAGE&quot;: {</span></span>
<span class="line"><span>                    &quot;type&quot;: &quot;BOOL&quot;,</span></span>
<span class="line"><span>                    &quot;value&quot;: &quot;True&quot;</span></span>
<span class="line"><span>                },</span></span></code></pre></div><p>算子编译部署后，会在算子包安装目录下的op_api目录生成单算子调用的头文件aclnn_<em>xx</em>.h和动态库libcust_opapi.so。</p><p>以默认安装场景为例，单算子调用的头文件.h和动态库libcust_opapi.so所在的目录结构，如下所示：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>├── opp    //算子库目录</span></span>
<span class="line"><span>│   ├── vendors     //自定义算子所在目录</span></span>
<span class="line"><span>│       ├── config.ini</span></span>
<span class="line"><span>│       └── vendor_name1   // 存储对应厂商部署的自定义算子，此名字为编译自定义算子安装包时配置的vendor_name，若未配置，默认值为customize</span></span>
<span class="line"><span>│           ├── op_api</span></span>
<span class="line"><span>│           │   ├── include</span></span>
<span class="line"><span>│           │   │  └── aclnn_xx.h</span></span>
<span class="line"><span>│           │   └── lib</span></span>
<span class="line"><span>│           │       └── libcust_opapi.so</span></span>
<span class="line"><span>...</span></span></code></pre></div></li><li><p>对于算子动态库编译场景，参考<a href="./算子动态库和静态库编译.html">算子动态库和静态库编译</a>完成算子的编译。编译完成后会在如下路径生成单算子调用的头文件aclnn_<em>xx</em>.h和动态库libcust_opapi.so。其中CMAKE_INSTALL_PREFIX为开发者在cmake文件中配置的编译产物存放路径。</p><ul><li>动态库路径：\${CMAKE_INSTALL_PREFIX}/op_api/lib/libcust_opapi.so</li><li>头文件路径：\${CMAKE_INSTALL_PREFIX}/op_api/include</li></ul></li></ul><h2 id="准备验证代码工程" tabindex="-1">准备验证代码工程 <a class="header-anchor" href="#准备验证代码工程" aria-label="Permalink to &quot;准备验证代码工程&quot;">​</a></h2><p>代码工程目录结构如下，您可以单击<a href="https://gitcode.com/cann/asc-devkit/tree/master/examples/01_simd_cpp_api/02_features/01_invocation/aclnn_invocation" target="_blank" rel="noreferrer">LINK</a>，获取样例工程的完整样例：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>├── CMakeLists.txt    // 编译规则文件</span></span>
<span class="line"><span>├── main.cpp    // 单算子调用主体流程实现文件</span></span></code></pre></div><h2 id="单算子调用流程" tabindex="-1">单算子调用流程 <a class="header-anchor" href="#单算子调用流程" aria-label="Permalink to &quot;单算子调用流程&quot;">​</a></h2><p>单算子API执行流程如下：</p><p><strong>图 1</strong> 单算子API执行接口调用流程<br><img src="`+l+`" alt="" title="单算子API执行接口调用流程"></p><p>本节以<strong>AddCustom自定义算子</strong>调用为例，介绍如何编写单算子调用的代码逻辑。其他算子的调用逻辑与Add算子大致一样，请根据实际情况自行修改代码。</p><p>以下是关键步骤的代码示例，不可以直接拷贝编译运行，仅供参考，调用接口后，需增加异常处理的分支，并记录报错日志、提示日志，此处不一一列举。</p><div class="note custom-block github-alert"><p class="custom-block-title">说明</p><p>因为单算子API执行方式，会自动在编译工程的build_out/autogen目录下生成.cpp和.h，编写单算子的调用代码时，要包含自动生成的单算子API执行接口头文件。示例如下：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &quot;aclnn_add_custom.h&quot;</span></span></code></pre></div></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>// 1.初始化</span></span>
<span class="line"><span>CHECK_ACL(aclnnInit(nullptr));</span></span>
<span class="line"><span></span></span>
<span class="line"><span>// 2.运行管理资源申请</span></span>
<span class="line"><span>const int32_t deviceId = 0;</span></span>
<span class="line"><span>CHECK_ACL(aclrtSetDevice(deviceId));</span></span>
<span class="line"><span></span></span>
<span class="line"><span>// 3.申请内存存放算子的输入输出</span></span>
<span class="line"><span>// ......</span></span>
<span class="line"><span></span></span>
<span class="line"><span>// 4.传输数据</span></span>
<span class="line"><span>CHECK_ACL(aclrtMemcpy(input0DeviceMem, bufferSize, input0HostData.data(), bufferSize, ACL_MEMCPY_HOST_TO_DEVICE));</span></span>
<span class="line"><span>CHECK_ACL(aclrtMemcpy(input1DeviceMem, bufferSize, input1HostData.data(), bufferSize, ACL_MEMCPY_HOST_TO_DEVICE));</span></span>
<span class="line"><span></span></span>
<span class="line"><span>// 5.计算workspace大小并申请内存</span></span>
<span class="line"><span>uint64_t workspaceSize = 0;</span></span>
<span class="line"><span>aclOpExecutor* executor = nullptr;</span></span>
<span class="line"><span>CHECK_ACL(aclnnAddCustomGetWorkspaceSize(input0, input1, output0, &amp;workspaceSize, &amp;executor));</span></span>
<span class="line"><span>void* workspaceDeviceMem = nullptr;</span></span>
<span class="line"><span>if (workspaceSize &gt; 0) {</span></span>
<span class="line"><span>    CHECK_ACL(aclrtMalloc(&amp;workspaceDeviceMem, workspaceSize, ACL_MEM_MALLOC_HUGE_FIRST));</span></span>
<span class="line"><span>}</span></span>
<span class="line"><span></span></span>
<span class="line"><span>// 6.执行算子</span></span>
<span class="line"><span>CHECK_ACL(aclnnAddCustom(workspaceDeviceMem, workspaceSize, executor, stream));</span></span>
<span class="line"><span></span></span>
<span class="line"><span>// 7.同步等待</span></span>
<span class="line"><span>CHECK_ACL(aclrtSynchronizeStream(stream));</span></span>
<span class="line"><span></span></span>
<span class="line"><span>// 8.处理执行算子后的输出数据，例如在屏幕上显示、写入文件等，由用户根据实际情况自行实现</span></span>
<span class="line"><span>// ......</span></span>
<span class="line"><span></span></span>
<span class="line"><span>// 9.释放运行管理资源</span></span>
<span class="line"><span>CHECK_ACL(aclrtResetDevice(deviceId));</span></span>
<span class="line"><span>// ....</span></span>
<span class="line"><span></span></span>
<span class="line"><span>// 10.去初始化</span></span>
<span class="line"><span>CHECK_ACL(aclnnFinalize());</span></span></code></pre></div><h2 id="cmakelists文件" tabindex="-1">CMakeLists文件 <a class="header-anchor" href="#cmakelists文件" aria-label="Permalink to &quot;CMakeLists文件&quot;">​</a></h2><p>算子编译后，会生成单算子调用的头文件aclnn_<em>xx</em>.h和动态库libcust_opapi.so。具体路径请参考<a href="#section29151158429">前置步骤</a>。</p><p>编译算子调用程序时，需要在头文件的搜索路径include_directories中增加单算子调用的头文件目录，便于找到该头文件；同时需要链接cust_opapi动态库并在库文件的搜索路径link_directories中增加libcust_opapi.so所在目录。</p><ul><li><p>在头文件的搜索路径include_directories中增加单算子调用的头文件目录。以下样例仅为参考，请根据头文件的实际目录位置进行设置。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>target_include_directories(execute_add_op PRIVATE</span></span>
<span class="line"><span>    $ENV{ASCEND_HOME_PATH}/include</span></span>
<span class="line"><span>    $ENV{ASCEND_OPP_PATH}/vendors/customize/op_api/include</span></span>
<span class="line"><span>)</span></span></code></pre></div></li><li><p>链接cust_opapi链接库。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>target_link_libraries(execute_add_op PRIVATE</span></span>
<span class="line"><span>    cust_opapi</span></span>
<span class="line"><span>    nnopbase</span></span>
<span class="line"><span>    acl_rt</span></span>
<span class="line"><span>)</span></span></code></pre></div></li><li><p>在库文件的搜索路径link_directories中增加libcust_opapi.so所在目录。以下样例仅为参考，请根据库文件的实际目录位置进行设置。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>target_link_directories(execute_add_op PRIVATE</span></span>
<span class="line"><span>    $ENV{ASCEND_HOME_PATH}/lib64</span></span>
<span class="line"><span>    $ENV{ASCEND_OPP_PATH}/vendors/customize/op_api/lib</span></span>
<span class="line"><span>)</span></span></code></pre></div></li></ul><h2 id="编译与运行" tabindex="-1">编译与运行 <a class="header-anchor" href="#编译与运行" aria-label="Permalink to &quot;编译与运行&quot;">​</a></h2><ol><li><p>开发环境上，设置环境变量，配置单算子验证程序编译依赖的头文件与库文件路径，如下为设置环境变量的示例。\${INSTALL_DIR}请替换为CANN软件安装后文件存储路径。以root用户安装为例，安装后文件默认存储路径为：/usr/local/Ascend/cann。_{arch-os}_为运行环境的架构和操作系统，_arch_表示操作系统架构，_os_表示操作系统，例如x86_64-linux或aarch64-linux。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>export DDK_PATH=\${INSTALL_DIR}</span></span>
<span class="line"><span>export NPU_HOST_LIB=\${INSTALL_DIR}/{arch-os}/devlib</span></span></code></pre></div></li><li><p>编译样例工程，生成单算子验证可执行文件。</p><ol><li><p>切换到样例工程根目录，然后在样例工程根目录下执行如下命令创建目录用于存放编译文件，例如，创建的目录为“build“。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>mkdir -p build</span></span></code></pre></div></li><li><p>进入build目录，执行cmake编译命令，生成编译文件</p><p>命令示例如下所示：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>cd build</span></span>
<span class="line"><span>cmake ../src -DCMAKE_SKIP_RPATH=TRUE</span></span></code></pre></div></li><li><p>执行如下命令，生成可执行文件。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>make</span></span></code></pre></div><p>会在工程目录的output目录下生成可执行文件<strong>execute_add_op</strong>。</p></li></ol></li><li><p>执行单算子</p><ol><li><p>以运行用户（例如HwHiAiUser）拷贝开发环境中样例工程output目录下的<strong>execute_add_op</strong>到运行环境任一目录。</p><p>说明： 若您的开发环境即为运行环境，此拷贝操作可跳过。</p></li><li><p>在运行环境中，执行<strong>execute_add_op</strong>文件：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>chmod +x execute_add_op</span></span>
<span class="line"><span>./execute_add_op</span></span></code></pre></div><p>如果有test pass，表明执行成功。</p></li></ol></li></ol>`,31)])])}const g=s(t,[["render",i]]);export{_ as __pageData,g as default};
