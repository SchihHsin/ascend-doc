import{c as s,Q as n,j as t,m as e}from"./chunks/framework.DOi4mjdC.js";const p="/assets/%E9%9D%9E%E5%AF%B9%E9%BD%90%E6%90%AC%E5%85%A5.DRelnHJZ.png",l="/assets/%E9%9D%9E%E5%AF%B9%E9%BD%90%E6%90%AC%E5%87%BA.D_4Pondr.png",i="/assets/%E7%9F%A2%E9%87%8F%E8%AE%A1%E7%AE%97%E8%B5%B7%E5%A7%8B%E5%9C%B0%E5%9D%80%E9%9D%9E32%E5%AD%97%E8%8A%82%E5%AF%B9%E9%BD%90%E7%9A%84%E9%94%99%E8%AF%AF%E7%A4%BA%E4%BE%8B.D8xZ7ysk.png",o="/assets/%E9%9D%9E%E5%AF%B9%E9%BD%90%E5%A4%84%E7%90%86%E6%96%B9%E6%A1%88%E7%A4%BA%E6%84%8F%E5%9B%BE.B9o-Q2oX.png",c="/assets/%E5%86%97%E4%BD%99%E6%95%B0%E6%8D%AE%E5%8F%82%E4%B8%8E%E8%AE%A1%E7%AE%97.DtHyqjVh.png",r="/assets/%E4%BD%BF%E7%94%A8mask%E6%8E%A9%E6%8E%89%E8%84%8F%E6%95%B0%E6%8D%AE.DJoG5zw6.png",d="/assets/%E9%80%9A%E8%BF%87Duplicate%E9%80%90%E8%A1%8C%E6%B8%85%E9%9B%B6.D4ZMKv7W.png",u="/assets/%E9%80%9A%E8%BF%87Pad%E4%B8%80%E6%AC%A1%E6%80%A7%E6%B8%85%E9%9B%B6.BI9C7gC5.png",h="/assets/%E4%BD%BF%E7%94%A8UnPad%E6%8E%A5%E5%8F%A3%E5%8E%BB%E9%99%A4%E5%86%97%E4%BD%99%E6%95%B0%E6%8D%AE%E5%90%8E%E6%90%AC%E5%87%BA.Bc3XY36v.png",g="/assets/%E4%BD%BF%E7%94%A8GatherMask%E6%94%B6%E9%9B%86%E6%9C%89%E6%95%88%E6%95%B0%E6%8D%AE%E5%90%8E%E6%90%AC%E5%87%BA.lMWx0GNE.png",_="/assets/%E5%B8%A6%E5%86%97%E4%BD%99%E6%95%B0%E6%8D%AE%E6%90%AC%E5%87%BA.DMkkdksB.png",B=JSON.parse('{"title":"非对齐场景","description":"","frontmatter":{},"headers":[{"level":1,"title":"非对齐场景","slug":"非对齐场景"},{"level":2,"title":"数据搬运和Vector计算的对齐要求","slug":"数据搬运和vector计算的对齐要求"},{"level":2,"title":"非对齐处理方案","slug":"非对齐处理方案"},{"level":2,"title":"样例介绍","slug":"样例介绍"}],"relativePath":"guide/算子实践参考/SIMD算子实现/矢量编程/非对齐场景.md","filePath":"guide/算子实践参考/SIMD算子实现/矢量编程/非对齐场景.md"}'),m={name:"guide/算子实践参考/SIMD算子实现/矢量编程/非对齐场景.md"};function E(b,a,L,C,v,k){return n(),t("div",null,[...a[0]||(a[0]=[e('<h1 id="非对齐场景" tabindex="-1">非对齐场景 <a class="header-anchor" href="#非对齐场景" aria-label="Permalink to &quot;非对齐场景&quot;">​</a></h1><p>本节介绍非32字节对齐数据的更多处理方式，包括数据搬入、计算和搬出的处理。用户在实际算子开发中，可以参考如下方案介绍和算子样例灵活地处理非对齐场景。</p><h2 id="数据搬运和vector计算的对齐要求" tabindex="-1">数据搬运和Vector计算的对齐要求 <a class="header-anchor" href="#数据搬运和vector计算的对齐要求" aria-label="Permalink to &quot;数据搬运和Vector计算的对齐要求&quot;">​</a></h2><div class="note custom-block github-alert"><p class="custom-block-title">说明</p><p>进行数据搬运和Vector计算时，对于搬运的数据长度和操作数的起始地址有如下的对齐要求：</p><ul><li>使用DataCopy接口进行数据搬运，搬运的数据长度和操作数的起始地址（UB上）必须保证32字节对齐。</li><li>通常情况下，进行Vector计算时，操作数的起始地址必须保证32字节对齐。具体对齐要求需要查阅对应的API参考进行确认。</li></ul></div><p>下文描述中的Global指Global Memory上的tensor，Local指Local Memory上的tensor。</p><p>下面是一些非对齐搬运和计算的例子。</p><ul><li><p><strong>非对齐搬入</strong></p><p>当需要从Global拷贝11个half数值到Local时，为保证搬运的数据长度32字节对齐，使用DataCopy拷贝16个half（32B）数据到Local上，Local[11]~Local[15]被写成无效数据-1。</p><p><strong>图 1</strong> 非对齐搬入<br><img src="'+p+'" alt="" title="非对齐搬入"></p></li><li><p><strong>非对齐搬出</strong></p><p>当需要从Local拷贝11个half数值到Global时，为保证搬运的数据长度32字节对齐，使用DataCopy拷贝16个half（32B）数据到Global上，Global[11]~Global[15]被写成无效数据-1。</p><p><strong>图 2</strong> 非对齐搬出<br><img src="'+l+'" alt="" title="非对齐搬出"></p></li><li><p><strong>矢量计算起始地址非32字节对齐的错误示例</strong></p><p>矢量计算时需要保证起始地址32字节对齐，如下的示例中，从Local1[7]，即LocalTensor的第8个数开始计算，起始地址不满足32字节对齐，是错误示例。</p><p><strong>图 3</strong> 矢量计算起始地址非32字节对齐的错误示例<br><img src="'+i+'" alt="" title="矢量计算起始地址非32字节对齐的错误示例"></p></li></ul><h2 id="非对齐处理方案" tabindex="-1">非对齐处理方案 <a class="header-anchor" href="#非对齐处理方案" aria-label="Permalink to &quot;非对齐处理方案&quot;">​</a></h2><p>DataCopyPad接口提供非对齐搬运的功能，如果基于该接口支持的产品开发算子（参见产品支持情况），则可以直接使用该接口解决非对齐场景下的搬运问题。使用DataCopyPad的完整示例请参考<a href="https://gitcode.com/cann/asc-devkit/tree/master/examples/01_simd_cpp_api/02_features/03_basic_api/00_data_movement/data_copy_pad_gm2ub_ub2gm" target="_blank" rel="noreferrer">DataCopyPad样例</a>。</p><p>部分型号不支持DataCopyPad接口，需要参考如下的方案处理。</p><p><strong>图 4</strong> 非对齐处理方案示意图<br><img src="'+o+'" alt="" title="非对齐处理方案示意图"></p><p>由于搬入时搬运的数据长度必须保证32字节对齐。数据长度非对齐的情况下，从Global逐行搬运Tensor数据到Local中，Local中每行都存在冗余数据。</p><p>搬入后，进行矢量计算时对冗余数据的处理方式有以下几种：</p><ul><li><strong>冗余数据参与计算</strong>。一般用于elewise计算场景。</li><li><strong>通过mask参数掩掉冗余数据</strong>。一般用于轴归约计算等场景。</li><li><strong>通过Duplicate逐行清零</strong>。计算前，针对每一行数据，调用基础API <a href="https://gitcode.com/cann/asc-devkit/blob/master/docs/api/context/Duplicate.md" target="_blank" rel="noreferrer">Duplicate</a>对冗余数据位置填充0值。</li><li><strong>通过Pad一次性清零</strong>。计算前，针对多行数据，可以采用高阶API <a href="https://gitcode.com/cann/asc-devkit/blob/master/docs/api/context/Pad.md" target="_blank" rel="noreferrer">Pad</a>接口对冗余数据一次性清零。</li></ul><p>由于搬出时搬运的数据长度和操作数的起始地址（UB上）必须保证32字节对齐，搬出时可以选择去除冗余数据或者带着冗余数据搬出的方式。</p><ul><li><strong>使用UnPad接口去除冗余数据后搬出</strong>。待搬出的有效数据总长度满足32字节对齐时，可使用高阶API <a href="https://gitcode.com/cann/asc-devkit/blob/master/docs/api/context/UnPad.md" target="_blank" rel="noreferrer">UnPad</a>接口去除冗余数据并完整搬出。</li><li><strong>使用GatherMask收集有效数据后搬出</strong>。待搬出的有效数据总长度大于等于32字节时，可使用<a href="https://gitcode.com/cann/asc-devkit/blob/master/docs/api/context/GatherMask.md" target="_blank" rel="noreferrer">GatherMask</a>重新收集有效数据，保证搬出的有效数据起始地址和数据长度32字节对齐。</li><li><strong>带冗余数据搬出</strong>。注意多核处理时开启原子加（使用<a href="https://gitcode.com/cann/asc-devkit/blob/master/docs/api/context/SetAtomicAdd.md" target="_blank" rel="noreferrer">SetAtomicAdd</a>接口），防止数据踩踏。</li></ul><p>下面分别对上述几种处理方案做详细说明。</p><ul><li><p>冗余数据参与计算</p><p>如下图所示，对前11个half数据进行Abs计算，冗余数据可以参与计算，不影响最终结果。步骤为：</p><ol><li>使用DataCopy从Global搬运16个half数据到Local1中，包含冗余数据-11~-15；</li><li>直接使用Abs做整块计算，不用计算尾块大小，冗余数据参与计算。</li></ol><p><strong>图 5</strong> 冗余数据参与计算<br><img src="'+c+'" alt="" title="冗余数据参与计算"></p></li><li><p>使用mask掩掉冗余数据</p><p>如下图所示，假设输入数据的shape为16 * 4，将输入数据搬入到UB后每行数据前4个half数据为有效数据，其余为冗余数据。为只对前4个half数据进行ReduceMin计算，可以通过设置mask参数的方法掩掉冗余数据。针对每行数据的处理步骤为：</p><ol><li>使用DataCopy从Global搬运16个half数据到Local1中；</li><li>对归约计算的目的操作数Local2清零，如使用Duplicate等；</li><li>进行归约操作，将ReduceMin的mask模式设置为前4个数据有效，从而掩掉冗余数据。</li></ol><p><strong>图 6</strong> 使用mask掩掉脏数据<br><img src="'+r+`" alt="" title="使用mask掩掉脏数据"></p></li><li><p>通过Duplicate逐行清零。</p><p>如下图所示，对于搬入后的非对齐数据，逐行进行Duplicate清零处理，步骤为：</p><ol><li><p>使用DataCopy从Global搬运16个half数据到Local中；</p></li><li><p>使用基础API Duplicate，按照如下方式设置mask值，控制仅后5个元素位置有效，将冗余数据填充为0。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>uint64_t mask0 = ((uint64_t)1 &lt;&lt; 16) - ((uint64_t)1 &lt;&lt; 11); </span></span>
<span class="line"><span>uint64_t mask[2] = {mask0, 0};</span></span></code></pre></div></li></ol><p><strong>图 7</strong> 通过Duplicate逐行清零<br><img src="`+d+'" alt="" title="通过Duplicate逐行清零"></p></li><li><p>通过Pad一次性清零。</p><p>如下图所示，假设输入数据的shape为16 * 6，搬入Local后大小为16 * 16，每行都包含冗余数据，逐行清零性能较差，可以使用Pad一次性清零，步骤为：</p><ol><li>将16 * 6的数据从GM上逐行搬入UB后，每行有6个有效数据；</li><li>使用Pad接口将冗余数据位置填充为0。（对应Pad接口使用场景为：tensor的width已32B对齐，但是有部分冗余数据）。</li></ol><p><strong>图 8</strong> 通过Pad一次性清零<br><img src="'+u+'" alt="" title="通过Pad一次性清零"></p></li><li><p>使用UnPad接口去除冗余数据后搬出。</p><p>如下图所示，Local内存大小为16*16，每行中只有前6个数为有效数据， 要搬出的有效数据16 * 6满足32B对齐，可以使用UnPad接口去除冗余数据并完整搬出。步骤如下：</p><ol><li>使用UnPad高阶API去除冗余值；</li><li>使用DataCopy搬运出连续的16 * 6个half数据到Global中。</li></ol><p><strong>图 9</strong> 使用UnPad接口去除冗余数据后搬出<br><img src="'+h+'" alt="" title="使用UnPad接口去除冗余数据后搬出"></p></li><li><p>使用GatherMask收集有效数据后搬出。</p><p>如下图所示，为搬出19个half数据到Global中，有16-18这3个数据的搬运无法满足对齐要求，使用GatherMask对有效数据进行重新收集，收集3-18这16个数据并搬出。步骤如下：</p><ol><li>完整拷贝前16个half（32B）数据到Global中；</li><li>使用GatherMask接口，将Local1[3]~[18]的数Gather到Local2中，Local2从对齐地址开始；</li><li>从Local2中搬运Gather的数据（32B整数倍）到Global中。</li></ol><p><strong>图 10</strong> 使用GatherMask收集有效数据后搬出。<br><img src="'+g+'" alt="" title="使用GatherMask收集有效数据后搬出"></p></li><li><p>带冗余数据搬出</p><p>如下图所示，有4个核参与计算，每个核拷贝出4个数，每个核上拷贝的数据长度不满足32字节对齐，采用将冗余数据一起搬出的方式，步骤如下：</p><ol><li><p>将目标Global完整清零，可以通过在host清零或者在kernel侧用UB覆盖的方式处理；</p></li><li><p>将本核内的Local数据，除了要搬出的4个有效数据，其余冗余部分清零（使用Duplicate接口）；</p></li><li><p>使用原子累加的方式拷贝到Global，原子累加结合冗余数据清零，确保不会出现数据踩踏。</p><p><strong>图 11</strong> 带冗余数据搬出<br><img src="'+_+`" alt="" title="带冗余数据搬出"></p></li></ol></li></ul><h2 id="样例介绍" tabindex="-1">样例介绍 <a class="header-anchor" href="#样例介绍" aria-label="Permalink to &quot;样例介绍&quot;">​</a></h2><ul><li><p>样例一：<a href="https://gitcode.com/cann/asc-devkit/blob/master/examples/01_simd_cpp_api/02_features/03_basic_api/02_memory_vector_compute/abs_gather_mask" target="_blank" rel="noreferrer">冗余数据参与计算+使用GatherMask收集有效数据后搬出</a>。</p><p>本样例中展示了shape为128 * 18的tensor进行Abs计算的算子实现。针对每行数据的处理方案如下：</p><p>搬入后，每行数据的后14个数为冗余数据。Abs接口入参BLOCKLEN_CEIL为32个数，是18个数进行32字节对齐后的结果，有14个冗余数据参与计算。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>AscendC::Abs(outputLocal, inputLocal, BLOCKLEN_CEIL); // main calculation</span></span></code></pre></div><p>计算完成后，通过GatherMask的bufPattern入参控制收集18个数中的后16个数。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>uint16_t tmpValue = 0;</span></span>
<span class="line"><span>AscendC::Duplicate&lt;uint16_t&gt;(bufPattern, tmpValue, 16);</span></span>
<span class="line"><span>bufPattern.SetValue(0, 0b1111111111111100); // select the last 14 elements of the first 16 positions</span></span>
<span class="line"><span>bufPattern.SetValue(1, 0b0000000000000011); // select the first 2 elements of the next 16 positions</span></span>
<span class="line"><span>uint32_t mask = 32;</span></span>
<span class="line"><span>uint64_t rsvdCnt = 0;</span></span>
<span class="line"><span>AscendC::LocalTensor&lt;half&gt; tailLocal = outQueueTail.AllocTensor&lt;half&gt;();</span></span>
<span class="line"><span>AscendC::GatherMask(tailLocal, outputLocal, bufPattern, true, mask, {1, 1, 8, 8}, rsvdCnt);</span></span></code></pre></div><p>首先使用DataCopy搬运前16个数，然后搬运后16个数，中间的14个数存在重复搬运。注意：因为DataCopy的目的地址存在重叠所以需要通过PipeBarrier添加流水同步。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>uint32_t copyLenMain = TILE_LENGTH * sizeof(half) / 32 * 32 / sizeof(half);</span></span>
<span class="line"><span>uint32_t offsetMain = progress * TILE_LENGTH;</span></span>
<span class="line"><span>AscendC::DataCopy(dstGlobal[offsetMain], outputLocal, copyLenMain);</span></span>
<span class="line"><span>AscendC::PipeBarrier&lt;PIPE_MTE3&gt;();</span></span>
<span class="line"><span>uint32_t tailLen = 32 / sizeof(half);</span></span>
<span class="line"><span>uint32_t offsetTail = offsetMain + (TILE_LENGTH - tailLen);</span></span>
<span class="line"><span>AscendC::DataCopy(dstGlobal[offsetTail], tailLocal, tailLen);</span></span></code></pre></div><p>搬入时要保证32字节对齐，所以要将输入的最后一行补齐到32字节对齐，防止访问非法数据，main.cpp中对GM上输入的长度的定义如下：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>size_t inputByteSize = 2318 * sizeof(int16_t); // 2318 = 2304 + 32 - 18</span></span>
<span class="line"><span>size_t outputByteSize = 2304 * sizeof(int16_t);</span></span></code></pre></div></li><li><p>样例二：<a href="https://gitcode.com/cann/asc-devkit/tree/master/examples/01_simd_cpp_api/02_features/03_basic_api/02_memory_vector_compute/abs_duplicate" target="_blank" rel="noreferrer">通过Duplicate逐行清零+带冗余数据搬出</a>。</p><p>本样例中展示了shape为64 * 11的tensor进行Abs计算的算子实现。 共使用4个核，每个核处理16 * 11个数据。</p><p>搬入后，每行数据的后5个数为冗余数据。通过Duplicate接口对每行数据中的后5个数据进行清零。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>// mask mode controls only the last 5 elements doing Duplicate</span></span>
<span class="line"><span>uint64_t mask0 = (1ul &lt;&lt; 16) - (1ul &lt;&lt; BLOCK_ELEMENT_NUM);</span></span>
<span class="line"><span>uint64_t mask[2] = {mask0, 0};</span></span>
<span class="line"><span>for (int32_t i = 0; i &lt; BLOCK_GROUP_NUM; i++) {</span></span>
<span class="line"><span>    AscendC::Duplicate&lt;half&gt;(inputLocal[i * BLOCKLEN_CEIL], 0, mask, 1, 1, 1); // clear dummy data on inputLocal</span></span>
<span class="line"><span>}</span></span>
<span class="line"><span>AscendC::Abs(outputLocal, inputLocal, BLOCKLEN_CEIL * BLOCK_GROUP_NUM);</span></span></code></pre></div><p>搬出时，带冗余数据搬出并开启原子累加，BLOCKLEN_CEIL中包含冗余数据。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>AscendC::SetAtomicAdd&lt;half&gt;();</span></span>
<span class="line"><span>for (int32_t i = 0; i &lt; BLOCK_GROUP_NUM; i++) {</span></span>
<span class="line"><span>    AscendC::DataCopy&lt;half&gt;(dstGlobal[i * BLOCK_ELEMENT_NUM], outputLocal[i * BLOCKLEN_CEIL], BLOCKLEN_CEIL);</span></span>
<span class="line"><span>}</span></span>
<span class="line"><span>AscendC::DisableDmaAtomic();</span></span></code></pre></div><p>所以在初始化时，需要对GM数据进行清零，清零代码如下，示例中多个核都调用<a href="https://gitcode.com/cann/asc-devkit/blob/master/docs/api/context/Fill-98.md" target="_blank" rel="noreferrer">Fill</a>接口进行清零，需要调用SyncAll进行核间同步。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>AscendC::Fill&lt;half&gt;(dstGlobal, blockLength, 0);</span></span>
<span class="line"><span></span></span>
<span class="line"><span>pipe.InitBuffer(inQueue, BUFFER_NUM, BLOCK_GROUP_NUM * BLOCKLEN_CEIL * sizeof(half));</span></span>
<span class="line"><span>pipe.InitBuffer(outQueue, BUFFER_NUM, BLOCK_GROUP_NUM * BLOCKLEN_CEIL * sizeof(half));</span></span>
<span class="line"><span>pipe.InitBuffer(syncLocalTbuf, USE_CORE_NUM * DEFAULT_SYNCALL_NEED_SIZE * sizeof(int32_t));</span></span>
<span class="line"><span>AscendC::LocalTensor&lt;int32_t&gt; SyncLocal = syncLocalTbuf.Get&lt;int32_t&gt;();</span></span>
<span class="line"><span>AscendC::SyncAll(syncGlobal, SyncLocal, USE_CORE_NUM);</span></span></code></pre></div><p>搬入时要保证32字节对齐，需要将输入的最后一行补齐到32字节对齐，防止访问非法数据；搬出时带冗余数据搬出，输出的最后一行也需要补齐到32字节对齐。main.cpp中对GM上输入输出的长度的定义如下：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>uint32_t elemLength = 709; // TOTAL_LENGTH + (BLOCKLEN_CEIL - BLOCK_ELEMENT_NUM)</span></span>
<span class="line"><span>// copy in borrow the next (BLOCKLEN_CEIL - BLOCK_ELEMENT_NUM) elements of srcGM</span></span>
<span class="line"><span>size_t inputByteSize = elemLength * sizeof(int16_t);</span></span>
<span class="line"><span>// copy out atomic add extra (BLOCKLEN_CEIL - BLOCK_ELEMENT_NUM) zeros to dstGM</span></span>
<span class="line"><span>size_t outputByteSize = elemLength * sizeof(int16_t);</span></span></code></pre></div></li><li><p>样例三：<a href="https://gitcode.com/cann/asc-devkit/tree/master/examples/01_simd_cpp_api/02_features/03_basic_api/02_memory_vector_compute/abs_unpad" target="_blank" rel="noreferrer">冗余数据参与计算+使用UnPad接口去除冗余数据后搬出</a>。</p><p>本样例中展示了shape为2048 * 14的tensor进行Abs计算的算子实现。 共使用8个核，每个核处理256 * 14个数据。</p><p>搬入后，每行数据的后2个数为冗余数据。Abs接口入参BLOCK_GROUP_NUM * BLOCKLEN_CEIL为连续的16行数据，每行16个数，每行的冗余数据参与计算。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>AscendC::Abs(inputLocal, inputLocal, BLOCK_GROUP_NUM * BLOCKLEN_CEIL); // main calculation</span></span></code></pre></div><p>计算后，使用UnPad接口去除冗余数据后再搬出，通过unPadParams.rightPad参数控制去除每行最后的2个冗余数据。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>unPadParams.rightPad = BLOCKLEN_CEIL - BLOCK_ELEMENT_NUM; // delete 2 dummy half each row</span></span>
<span class="line"><span>AscendC::UnPad&lt;half&gt;(outputLocal, inputLocal, unPadParams, this-&gt;tiling);</span></span></code></pre></div><p>注意：UnPad接口需要传入tiling参数。abs_unpad_tiling.cpp中关键计算过程如下：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>AscendC::GetUnPadMaxMinTmpSize(*ascendcPlatform, srcShape, sizeof(int16_t), tmpMaxSize, tmpMinSize);</span></span>
<span class="line"><span>optiling::UnPadTiling tilingData;</span></span>
<span class="line"><span>AscendC::UnPadTilingFunc(srcShape, tmpMaxSize, sizeof(int16_t), tilingData);</span></span></code></pre></div><p>tiling参数需要通过核函数的入参传入到kernel侧，供UnPad高阶API使用。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>abs_unpad_custom&lt;&lt;&lt;numBlocks, nullptr, stream&gt;&gt;&gt;(xDevice, zDevice, tilingDevice);</span></span></code></pre></div><p>搬入时要保证32字节对齐，所以要将输入的最后一行补齐到32字节对齐，防止访问非法数据，main.cpp中对GM上输入的长度的定义如下。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>// 28674 is TOTAL_LENGTH + (BLOCKLEN_CEIL - BLOCK_ELEMENT_NUM)</span></span>
<span class="line"><span>// 28672 is TOTAL_LENGTH</span></span>
<span class="line"><span>// copy in borrow the next (BLOCKLEN_CEIL - BLOCK_ELEMENT_NUM) elements of srcGM</span></span>
<span class="line"><span>uint32_t oriLength = 28672;</span></span>
<span class="line"><span>uint32_t colNum = 14;</span></span>
<span class="line"><span>uint32_t maxColNum = 32 / sizeof(uint16_t);</span></span>
<span class="line"><span>uint32_t padLength = oriLength + maxColNum - colNum;</span></span>
<span class="line"><span>size_t inputByteSize = padLength * sizeof(int16_t);</span></span>
<span class="line"><span>size_t outputByteSize = oriLength * sizeof(int16_t);</span></span></code></pre></div></li><li><p>示例四：<a href="https://gitcode.com/cann/asc-devkit/tree/master/examples/01_simd_cpp_api/02_features/03_basic_api/02_memory_vector_compute/abs_pad" target="_blank" rel="noreferrer">通过Pad一次性清零+带冗余数据搬出</a>。</p><p>本样例中展示了shape为2048 * 7的tensor进行Abs计算的算子实现。 共使用8个核，每个核处理256 * 7个数据。</p><p>搬入后，每行数据的后9个数为冗余数据。每个核上通过Pad接口将256 * 9的冗余数据块整体清零后参与Abs计算。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>AscendC::PadParams padParams = {0, BLOCKLEN_CEIL - BLOCK_ELEMENT_NUM, 0};</span></span>
<span class="line"><span>AscendC::Pad(outputLocal, inputLocal, padParams, this-&gt;tiling);</span></span>
<span class="line"><span>AscendC::Abs(outputLocal, outputLocal, BLOCK_GROUP_NUM * BLOCKLEN_CEIL); // main calculation</span></span></code></pre></div><p>计算后带冗余数据搬出的代码解释和<a href="#zh-cn_topic_0000001945665465_li1891184254913">样例二</a>一致。</p><p>注意：Pad接口需要传入tiling参数。abs_pad_tiling.cpp中关键计算过程如下：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>AscendC::GetPadMaxMinTmpSize(srcShape, sizeof(int16_t), tmpMaxSize, tmpMinSize); </span></span>
<span class="line"><span>optiling::PadTiling tilingData;</span></span>
<span class="line"><span>AscendC::PadTilingFunc(srcShape, oriSrcShape, tmpMaxSize, sizeof(int16_t), tilingData);</span></span></code></pre></div><p>tiling参数需要通过核函数的入参传入到kernel侧，供Pad高阶API使用。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>abs_pad_custom&lt;&lt;&lt;numBlocks, nullptr, stream&gt;&gt;&gt;(xDevice, zDevice, tilingDevice);</span></span></code></pre></div><p>搬入时要保证32字节对齐，需要将输入的最后一行补齐到32字节对齐，防止访问非法数据；搬出时带冗余数据搬出，输出的最后一行也需要补齐到32字节对齐。main.cpp中对GM上输入输出的长度的定义如下：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>// 14336 is the length of input data</span></span>
<span class="line"><span>uint32_t oriLength = 14336;</span></span>
<span class="line"><span>// we must allocate more space to prevent invalid address access</span></span>
<span class="line"><span>uint32_t padLength = oriLength + shapePad[1] - shapeUsed[1];</span></span>
<span class="line"><span>size_t inputByteSize = padLength * sizeof(int16_t);</span></span>
<span class="line"><span>size_t outputByteSize = padLength * sizeof(int16_t);</span></span>
<span class="line"><span>// however, original length must be used when output to file</span></span>
<span class="line"><span>size_t outputFileSize = oriLength * sizeof(int16_t);</span></span></code></pre></div></li><li><p>样例五：<a href="https://gitcode.com/cann/asc-devkit/tree/master/examples/01_simd_cpp_api/02_features/03_basic_api/02_memory_vector_compute/reduce_min" target="_blank" rel="noreferrer">使用mask掩掉冗余数据+带冗余数据搬出</a>。</p><p>本样例中展示了shape为16 * 4的tensor每行数据进行ReduceMin计算的算子实现。 共使用4个核，每个核处理4 * 4个数据。</p><p>搬入后，每行数据的后12个数为冗余数据。通过ReduceMin的入参Mask控制只有前4个数参与计算。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>uint64_t Mask0 = ((uint64_t)1 &lt;&lt; BLOCK_ELEMENT_NUM) - 1; // mask mode controls only the first 4 elements do ReduceMin calculation</span></span>
<span class="line"><span>uint64_t Mask[2] = {Mask0, 0};</span></span>
<span class="line"><span>// main calculation</span></span>
<span class="line"><span>for (int i = 0; i &lt; BLOCK_GROUP_NUM; i++) {</span></span>
<span class="line"><span>    AscendC::ReduceMin&lt;half&gt;(outputLocal[i * BLOCKLEN_CEIL], inputLocal[i * BLOCKLEN_CEIL], workLocal, Mask, 1, 8, false);</span></span>
<span class="line"><span>}</span></span>
<span class="line"><span>outQueue.EnQue&lt;half&gt;(outputLocal);</span></span>
<span class="line"><span>inQueue.FreeTensor(inputLocal);</span></span></code></pre></div><p>计算后带冗余数据搬出的代码解释和<a href="#zh-cn_topic_0000001945665465_li1891184254913">样例二</a>一致。</p><p>搬入时要保证32字节对齐，需要将输入的最后一行补齐到32字节对齐，防止访问非法数据；搬出时带冗余数据搬出，输出的最后一行也需要补齐到32字节对齐。main.cpp中对GM上输入输出的长度的定义如下：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>// copy in borrow the next (BLOCKLEN_CEIL - BLOCK_ELEMENT_NUM) elements of srcGM</span></span>
<span class="line"><span>size_t inputByteSize = 76 * sizeof(int16_t);</span></span>
<span class="line"><span>// copy out atomic add extra (BLOCKLEN_CEIL - BLOCK_ELEMENT_NUM) zeros to dstGM</span></span>
<span class="line"><span>size_t outputByteSize = 76 * sizeof(int16_t);</span></span></code></pre></div></li></ul>`,20)])])}const A=s(m,[["render",E]]);export{B as __pageData,A as default};
