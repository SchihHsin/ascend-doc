import{_ as s}from"./chunks/NPU侧运行验证步骤.BFaRF1G3.js";import{c as a,Q as n,j as l,m as e}from"./chunks/framework.DOi4mjdC.js";const p="/assets/CPU%E4%BE%A7%E8%BF%90%E8%A1%8C%E9%AA%8C%E8%AF%81%E6%AD%A5%E9%AA%A4.B269LQ3y.png",i="/assets/%E7%BC%96%E8%AF%91%E7%AE%80%E5%8C%96%E6%B5%81%E7%A8%8B%E5%9B%BE.B93uVRor.png",o="/assets/%E4%B8%80%E9%94%AE%E5%BC%8F%E7%BC%96%E8%AF%91%E8%BF%90%E8%A1%8C%E8%84%9A%E6%9C%AC%E6%B5%81%E7%A8%8B%E5%9B%BE.ZiyZhwFP.png",c="/assets/CPU%E5%9F%9F%E5%92%8CNPU%E5%9F%9F%E7%9A%84%E6%A0%B8%E5%87%BD%E6%95%B0%E8%BF%90%E8%A1%8C%E9%80%BB%E8%BE%91%E5%AF%B9%E6%AF%94.D43fjEmo.png",v=JSON.parse('{"title":"基于样例工程完成Kernel直调","description":"","frontmatter":{},"headers":[{"level":1,"title":"基于样例工程完成Kernel直调","slug":"基于样例工程完成kernel直调"},{"level":2,"title":"环境准备","slug":"环境准备"},{"level":2,"title":"工程目录","slug":"工程目录"},{"level":2,"title":"算子Kernel侧实现","slug":"算子kernel侧实现"},{"level":2,"title":"算子调用应用程序","slug":"算子调用应用程序"},{"level":2,"title":"CMake编译配置文件编写","slug":"cmake编译配置文件编写"},{"level":2,"title":"输入数据和真值数据生成以及验证脚本文件","slug":"输入数据和真值数据生成以及验证脚本文件"},{"level":1,"title":"-*- coding:utf-8 -*-","slug":"codingutf-8"},{"level":2,"title":"修改并执行一键式编译运行脚本","slug":"修改并执行一键式编译运行脚本"},{"level":2,"title":"CPU侧验证核函数","slug":"cpu侧验证核函数"}],"relativePath":"guide/编程指南/附录/基于样例工程完成Kernel直调.md","filePath":"guide/编程指南/附录/基于样例工程完成Kernel直调.md"}'),r={name:"guide/编程指南/附录/基于样例工程完成Kernel直调.md"};function u(g,t,d,_,q,C){return n(),l("div",null,[...t[0]||(t[0]=[e(`<h1 id="基于样例工程完成kernel直调" tabindex="-1">基于样例工程完成Kernel直调 <a class="header-anchor" href="#基于样例工程完成kernel直调" aria-label="Permalink to &quot;基于样例工程完成Kernel直调&quot;">​</a></h1><div class="note custom-block github-alert"><p class="custom-block-title">说明</p><p>本章节介绍的基于样例工程完成Kernel直调的方式，后续不再演进。推荐开发者直接使用命令行或者编写Cmake文件进行编译，详细内容请参考<a href="./../编译与运行/AI-Core-SIMD编译/算子编译简介.html">AI Core SIMD编译</a>。</p></div><p>下文将以Add矢量算子为例对Kernel直调算子开发流程进行详细介绍。</p><h2 id="环境准备" tabindex="-1">环境准备 <a class="header-anchor" href="#环境准备" aria-label="Permalink to &quot;环境准备&quot;">​</a></h2><ul><li><p>使用Kernel Launch算子工程之前，需要参考<a href="./../../入门教程/环境准备.html">环境准备</a>章节安装驱动固件和CANN软件包，完成开发环境和运行环境的准备。</p></li><li><p>使用该算子工程要求cmake版本为3.16及以上版本，如不符合要求，请参考如下的命令示例更新cmake版本，如下示例以更新到3.16.0版本为例。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>wget https://cmake.org/files/v3.16/cmake-3.16.0.tar.gz </span></span>
<span class="line"><span>tar -zxvf cmake-3.16.0.tar.gz </span></span>
<span class="line"><span>cd cmake-3.16.0</span></span>
<span class="line"><span> ./bootstrap --prefix=/usr </span></span>
<span class="line"><span>sudo make </span></span>
<span class="line"><span>sudo make install</span></span></code></pre></div></li></ul><h2 id="工程目录" tabindex="-1">工程目录 <a class="header-anchor" href="#工程目录" aria-label="Permalink to &quot;工程目录&quot;">​</a></h2><p>您可以单击<a href="https://gitee.com/ascend/samples/tree/master/operator/ascendc/0_introduction/3_add_kernellaunch/AddKernelInvocationNeo" target="_blank" rel="noreferrer">矢量算子样例</a>，获取核函数开发和运行验证的完整样例。样例目录结构如下所示：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>AddKernelInvocationNeo</span></span>
<span class="line"><span>|-- cmake                                                 // CMake编译文件</span></span>
<span class="line"><span>|-- scripts</span></span>
<span class="line"><span>|  ├── gen_data.py                                     // 输入数据和真值数据生成脚本文件</span></span>
<span class="line"><span>|  ├── verify_result.py                                // 验证输出数据和真值数据是否一致的验证脚本</span></span>
<span class="line"><span>|-- CMakeLists.txt                                        // CMake编译配置文件</span></span>
<span class="line"><span>|-- add_custom.cpp                                        // 矢量算子kernel实现</span></span>
<span class="line"><span>|-- data_utils.h                                          // 数据读入写出函数</span></span>
<span class="line"><span>|-- main.cpp                                              // 主函数，调用算子的应用程序，含CPU域及NPU域调用</span></span>
<span class="line"><span>|-- run.sh                                                // 编译运行算子的脚本</span></span></code></pre></div><p>基于该算子工程，开发者进行算子开发的步骤如下：</p><ul><li><p>完成算子kernel侧实现。</p></li><li><p>编写算子调用应用程序main.cpp。</p></li><li><p>编写CMake编译配置文件CMakeLists.txt。</p></li><li><p>根据实际需要修改输入数据和真值数据生成脚本文件gen_data.py；验证输出数据和真值数据是否一致的验证脚本verify_result.py。</p></li><li><p>根据实际需要修改编译运行算子的脚本run.sh并执行该脚本，完成算子的编译运行和结果验证。</p></li></ul><h2 id="算子kernel侧实现" tabindex="-1">算子Kernel侧实现 <a class="header-anchor" href="#算子kernel侧实现" aria-label="Permalink to &quot;算子Kernel侧实现&quot;">​</a></h2><p>请参考工程目录中的矢量算子、矩阵算子、融合算子的Kernel实现完成Ascend C算子实现文件的编写。</p><div class="note custom-block github-alert"><p class="custom-block-title">说明</p><p>一个算子Kernel实现文件中只支持定义一个核函数。</p></div><h2 id="算子调用应用程序" tabindex="-1">算子调用应用程序 <a class="header-anchor" href="#算子调用应用程序" aria-label="Permalink to &quot;算子调用应用程序&quot;">​</a></h2><p>下面代码以固定shape的add_custom算子为例，介绍算子核函数调用的应用程序main.cpp如何编写。您在实现自己的应用程序时，需要关注由于算子核函数不同带来的修改，包括算子核函数名，入参出参的不同等，合理安排相应的内存分配、内存拷贝和文件读写等，相关API的调用方式直接复用即可。</p><ol><li><p>按需包含头文件，通过ASCENDC_CPU_DEBUG宏区分CPU/NPU侧需要包含的头文件。需要注意的是，NPU侧需要包含对应的核函数调用接口声明所在的头文件aclrtlaunch_{kernel_name}.h（该头文件为工程框架自动生成），kernel_name为算子核函数的名称。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &quot;data_utils.h&quot;</span></span>
<span class="line"><span>#ifndef ASCENDC_CPU_DEBUG</span></span>
<span class="line"><span>#include &quot;acl/acl.h&quot;</span></span>
<span class="line"><span>#include &quot;aclrtlaunch_add_custom.h&quot;</span></span>
<span class="line"><span>#else</span></span>
<span class="line"><span>#include &quot;tikicpulib.h&quot;</span></span>
<span class="line"><span>extern &quot;C&quot; __global__ __aicore__ void add_custom(GM_ADDR x, GM_ADDR y, GM_ADDR z);</span></span>
<span class="line"><span>#endif</span></span></code></pre></div></li><li><p>应用程序框架编写。该应用程序通过ASCENDC_CPU_DEBUG宏区分代码逻辑运行于CPU侧还是NPU侧。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int32_t main(int32_t argc, char* argv[])</span></span>
<span class="line"><span>{</span></span>
<span class="line"><span>    uint32_t numBlocks = 8;</span></span>
<span class="line"><span>    size_t inputByteSize = 8 * 2048 * sizeof(uint16_t);</span></span>
<span class="line"><span>    size_t outputByteSize = 8 * 2048 * sizeof(uint16_t);</span></span>
<span class="line"><span></span></span>
<span class="line"><span>#ifdef ASCENDC_CPU_DEBUG</span></span>
<span class="line"><span>    // 用于CPU调试的调用程序</span></span>
<span class="line"><span>#else</span></span>
<span class="line"><span>    // NPU侧运行算子的调用程序</span></span>
<span class="line"><span>#endif</span></span>
<span class="line"><span>    return 0;</span></span>
<span class="line"><span>}</span></span></code></pre></div></li><li><p>CPU侧运行验证。完成算子核函数CPU侧运行验证的步骤如下：</p><p><strong>图 1</strong> CPU侧运行验证步骤<br><img src="`+p+`" alt="" title="CPU侧运行验证步骤"></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    // 使用GmAlloc分配共享内存，并进行数据初始化</span></span>
<span class="line"><span>    uint8_t* x = (uint8_t*)AscendC::GmAlloc(inputByteSize);</span></span>
<span class="line"><span>    uint8_t* y = (uint8_t*)AscendC::GmAlloc(inputByteSize);</span></span>
<span class="line"><span>    uint8_t* z = (uint8_t*)AscendC::GmAlloc(outputByteSize);</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    ReadFile(&quot;./input/input_x.bin&quot;, inputByteSize, x, inputByteSize);</span></span>
<span class="line"><span>    ReadFile(&quot;./input/input_y.bin&quot;, inputByteSize, y, inputByteSize);</span></span>
<span class="line"><span>    // 矢量算子需要设置内核模式为AIV模式</span></span>
<span class="line"><span>    AscendC::SetKernelMode(KernelMode::AIV_MODE);</span></span>
<span class="line"><span>    // 调用ICPU_RUN_KF调测宏，完成核函数CPU侧的调用</span></span>
<span class="line"><span>    ICPU_RUN_KF(add_custom, numBlocks, x, y, z);</span></span>
<span class="line"><span>    // 输出数据写出</span></span>
<span class="line"><span>    WriteFile(&quot;./output/output_z.bin&quot;, z, outputByteSize);</span></span>
<span class="line"><span>    // 调用GmFree释放申请的资源</span></span>
<span class="line"><span>    AscendC::GmFree((void *)x);</span></span>
<span class="line"><span>    AscendC::GmFree((void *)y);</span></span>
<span class="line"><span>    AscendC::GmFree((void *)z);</span></span></code></pre></div></li><li><p>NPU侧运行验证。完成算子核函数NPU侧运行验证的步骤如下：</p><p><strong>图 2</strong> NPU侧运行验证步骤<br><img src="`+s+`" alt="" title="NPU侧运行验证步骤"></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    // 初始化</span></span>
<span class="line"><span>    CHECK_ACL(aclInit(nullptr));</span></span>
<span class="line"><span>    // 运行管理资源申请</span></span>
<span class="line"><span>    int32_t deviceId = 0;</span></span>
<span class="line"><span>    CHECK_ACL(aclrtSetDevice(deviceId));</span></span>
<span class="line"><span>    aclrtStream stream = nullptr;</span></span>
<span class="line"><span>    CHECK_ACL(aclrtCreateStream(&amp;stream));</span></span>
<span class="line"><span>    // 分配Host内存</span></span>
<span class="line"><span>    uint8_t *xHost, *yHost, *zHost;</span></span>
<span class="line"><span>    uint8_t *xDevice, *yDevice, *zDevice;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    CHECK_ACL(aclrtMallocHost((void**)(&amp;xHost), inputByteSize));</span></span>
<span class="line"><span>    CHECK_ACL(aclrtMallocHost((void**)(&amp;yHost), inputByteSize));</span></span>
<span class="line"><span>    CHECK_ACL(aclrtMallocHost((void**)(&amp;zHost), outputByteSize));</span></span>
<span class="line"><span>    // 分配Device内存</span></span>
<span class="line"><span>    CHECK_ACL(aclrtMalloc((void**)&amp;xDevice, inputByteSize, ACL_MEM_MALLOC_HUGE_FIRST));</span></span>
<span class="line"><span>    CHECK_ACL(aclrtMalloc((void**)&amp;yDevice, inputByteSize, ACL_MEM_MALLOC_HUGE_FIRST));</span></span>
<span class="line"><span>    CHECK_ACL(aclrtMalloc((void**)&amp;zDevice, outputByteSize, ACL_MEM_MALLOC_HUGE_FIRST));</span></span>
<span class="line"><span>    // Host内存初始化</span></span>
<span class="line"><span>    ReadFile(&quot;./input/input_x.bin&quot;, inputByteSize, xHost, inputByteSize);</span></span>
<span class="line"><span>    ReadFile(&quot;./input/input_y.bin&quot;, inputByteSize, yHost, inputByteSize);</span></span>
<span class="line"><span>    // 将数据从Host上拷贝到Device上</span></span>
<span class="line"><span>    CHECK_ACL(aclrtMemcpy(xDevice, inputByteSize, xHost, inputByteSize, ACL_MEMCPY_HOST_TO_DEVICE));</span></span>
<span class="line"><span>    CHECK_ACL(aclrtMemcpy(yDevice, inputByteSize, yHost, inputByteSize, ACL_MEMCPY_HOST_TO_DEVICE));</span></span>
<span class="line"><span>    // 用内核调用符&lt;&lt;&lt;&gt;&gt;&gt;调用核函数完成指定的运算,add_custom_do中封装了&lt;&lt;&lt;&gt;&gt;&gt;调用</span></span>
<span class="line"><span>    add_custom_do(numBlocks, nullptr, stream, xDevice, yDevice, zDevice);</span></span>
<span class="line"><span>    // 用ACLRT_LAUNCH_KERNEL接口调用核函数完成指定的运算</span></span>
<span class="line"><span>    // ACLRT_LAUNCH_KERNEL(add_custom)(numBlocks, stream, xDevice, yDevice, zDevice);</span></span>
<span class="line"><span>    CHECK_ACL(aclrtSynchronizeStream(stream));</span></span>
<span class="line"><span>    // 将Device上的运算结果拷贝回Host</span></span>
<span class="line"><span>    CHECK_ACL(aclrtMemcpy(zHost, outputByteSize, zDevice, outputByteSize, ACL_MEMCPY_DEVICE_TO_HOST));</span></span>
<span class="line"><span>    WriteFile(&quot;./output/output_z.bin&quot;, zHost, outputByteSize);</span></span>
<span class="line"><span>    // 释放申请的资源</span></span>
<span class="line"><span>    CHECK_ACL(aclrtFree(xDevice));</span></span>
<span class="line"><span>    CHECK_ACL(aclrtFree(yDevice));</span></span>
<span class="line"><span>    CHECK_ACL(aclrtFree(zDevice));</span></span>
<span class="line"><span>    CHECK_ACL(aclrtFreeHost(xHost));</span></span>
<span class="line"><span>    CHECK_ACL(aclrtFreeHost(yHost));</span></span>
<span class="line"><span>    CHECK_ACL(aclrtFreeHost(zHost));</span></span>
<span class="line"><span>    // 去初始化</span></span>
<span class="line"><span>    CHECK_ACL(aclrtDestroyStream(stream));</span></span>
<span class="line"><span>    CHECK_ACL(aclrtResetDevice(deviceId));</span></span>
<span class="line"><span>    CHECK_ACL(aclFinalize());</span></span></code></pre></div><div class="note custom-block github-alert"><p class="custom-block-title">说明</p><p>针对&lt;&lt;&lt;&gt;&gt;&gt;调用方式在<a href="./../编程模型/AI-Core-SIMD编程/核函数.html">核函数</a>章节已有说明，这里仅对ACLRT_LAUNCH_KERNEL调用接口的使用方法介绍如下：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ACLRT_LAUNCH_KERNEL(kernel_name)(numBlocks, stream, argument list);</span></span></code></pre></div><ul><li>kernel_name：算子核函数的名称。</li><li>numBlocks：规定了核函数将会在几个核上执行。每个执行该核函数的核会被分配一个逻辑ID，即block_idx，可以在核函数的实现中调用<a href="https://gitcode.com/cann/asc-devkit/blob/master/docs/api/context/GetBlockIdx.md" target="_blank" rel="noreferrer">GetBlockIdx</a>来获取block_idx。</li><li>stream，类型为aclrtStream，stream用于维护一些异步操作的执行顺序，确保按照应用程序中的代码调用顺序在Device上执行。stream创建等管理接口请参考<a href="https://hiascend.com/document/redirect/CannCommunityRuntimeApi" target="_blank" rel="noreferrer">《Runtime运行时API》</a>。</li><li>argument list：参数列表，与核函数的参数列表保持一致。</li></ul></div></li></ol><h2 id="cmake编译配置文件编写" tabindex="-1">CMake编译配置文件编写 <a class="header-anchor" href="#cmake编译配置文件编写" aria-label="Permalink to &quot;CMake编译配置文件编写&quot;">​</a></h2><p>本节会介绍CMake文件中一些关键环境变量和Cmake命令参数的说明，通常情况下不需要开发者修改，但是这些参数可以帮助开发者更好的理解编译原理，方便有能力的开发者对Cmake进行定制化处理。</p><p><strong>表 1</strong> 环境变量说明</p><p>&lt;table&gt;&lt;thead align=&quot;left&quot;&gt;&lt;tr&gt;&lt;th class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;24.54%&quot;&gt;&lt;p&gt;环境变量&lt;/p&gt; &lt;/th&gt; &lt;th class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;75.46000000000001%&quot;&gt;&lt;p&gt;配置说明&lt;/p&gt; &lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt;&lt;tr&gt;&lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;24.54%&quot;&gt;&lt;p&gt;SOC_VERSION&lt;/p&gt; &lt;/td&gt; &lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;75.46000000000001%&quot;&gt;&lt;p&gt;AI处理器的型号。&lt;/p&gt; &lt;/td&gt; &lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;24.54%&quot;&gt;&lt;p&gt;ASCEND_CANN_PACKAGE_PATH&lt;/p&gt; &lt;/td&gt; &lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;75.46000000000001%&quot;&gt;&lt;p&gt;CANN软件包安装后的实际路径。&lt;/p&gt; &lt;/td&gt; &lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;24.54%&quot;&gt;&lt;p&gt;CMAKE_BUILD_TYPE&lt;/p&gt; &lt;/td&gt; &lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;75.46000000000001%&quot;&gt;&lt;p&gt;编译模式选项，可配置为：&lt;/p&gt; &lt;ul&gt;&lt;li&gt;“Release”，Release版本，不包含调试信息，编译最终发布的版本。&lt;/li&gt;&lt;li&gt;“Debug”，Debug版本，包含调试信息，便于开发者开发和调试。&lt;/li&gt;&lt;/ul&gt; &lt;/td&gt; &lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;24.54%&quot;&gt;&lt;p&gt;CMAKE_INSTALL_PREFIX&lt;/p&gt; &lt;/td&gt; &lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;75.46000000000001%&quot;&gt;&lt;p&gt;用于指定CMAKE执行install时，安装的路径前缀，执行install后编译产物（ascendc_library中指定的target以及对应的头文件）会安装在该路径下。默认路径为当前目录的out目录下。&lt;/p&gt; &lt;/td&gt; &lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;24.54%&quot;&gt;&lt;p&gt;CMAKE_CXX_COMPILER_LAUNCHER&lt;/p&gt; &lt;/td&gt; &lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;75.46000000000001%&quot;&gt;&lt;p&gt;用于配置C++语言编译器（如g++）、毕昇编译器的启动器程序为ccache，配置后即可开启cache缓存编译，&lt;span&gt;加速重复编译并提高构建效率&lt;/span&gt;。使用该功能前需要安装ccache。&lt;/p&gt; &lt;p&gt;配置方法如下，在对应的CMakeLists.txt进行设置：&lt;/p&gt; &lt;pre class=&quot;screen&quot;&gt;set(CMAKE_CXX_COMPILER_LAUNCHER &lt;launcher_program&gt;)&lt;/pre&gt; &lt;p&gt;其中&lt;launcher_program&gt;是ccache的安装路径，比如ccache的安装路径为/usr/bin/ccache，示例如下：&lt;/p&gt; &lt;pre class=&quot;screen&quot;&gt;set(CMAKE_CXX_COMPILER_LAUNCHER /usr/bin/ccache)&lt;/pre&gt; &lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt;</p><p><strong>表 2</strong> Cmake命令语法说明</p><p>&lt;table&gt;&lt;thead align=&quot;left&quot;&gt;&lt;tr&gt;&lt;th class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;24.67%&quot;&gt;&lt;p&gt;Cmake命令&lt;/p&gt; &lt;/th&gt; &lt;th class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;75.33%&quot;&gt;&lt;p&gt;语法说明&lt;/p&gt; &lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt;&lt;tr&gt;&lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;24.67%&quot;&gt;&lt;p&gt;add_executable&lt;/p&gt; &lt;/td&gt; &lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;75.33%&quot;&gt;&lt;p&gt;使用指定的源文件将可执行文件添加到项目中。和Cmake通用的命令参数使用方法一致。&lt;/p&gt; &lt;/td&gt; &lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;24.67%&quot;&gt;&lt;p&gt;ascendc_library&lt;/p&gt; &lt;/td&gt; &lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;75.33%&quot;&gt;&lt;p&gt;使用指定的核函数源文件向项目（project）添加库。语法格式如下：&lt;/p&gt; &lt;pre class=&quot;screen&quot;&gt;ascendc_library(&lt;target_name&gt; [STATIC &lt;/span&gt;&lt;/span&gt;&lt;/span&gt;| SHARED] [&lt;source&gt;...]) &lt;/pre&gt; &lt;p&gt;其中&lt;target_name&gt;表示库文件的名字，该库文件会根据命令里列出的源文件来建立。STATIC、SHARED的作用是指定生成的库文件的类型。STATIC库是目标文件的归档文件，在连接其它目标的时候使用。SHARED库会被动态连接（动态连接库），在运行时会被加载。&lt;source&gt;表示核函数源文件。&lt;/p&gt; &lt;/td&gt; &lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;24.67%&quot;&gt;&lt;p&gt;ascendc_fatbin_library&lt;/p&gt; &lt;/td&gt; &lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;75.33%&quot;&gt;&lt;p&gt;使用指定的核函数源文件编译生成一个Kernel二进制文件，供Kernel加载和执行接口使用。语法格式如下：&lt;/p&gt; &lt;pre class=&quot;screen&quot;&gt;ascendc_fatbin_library(&lt;target_name&gt; [&lt;source&gt;...]) &lt;/pre&gt; &lt;ul&gt;&lt;li&gt;&lt;target_name&gt;表示库文件的名字，该库文件会根据命令里列出的核函数源文件编译生成&lt;target_name&gt;.o文件，放置于\${CMAKE_INSTALL_PREFIX}/fatbin/\${target_name}/路径下。&lt;/li&gt;&lt;li&gt;&lt;source&gt;表示核函数源文件。&lt;/li&gt;&lt;/ul&gt; &lt;div class=&quot;note&quot;&gt;&lt;span class=&quot;notetitle&quot;&gt; 说明： &lt;div class=&quot;notebody&quot;&gt;&lt;ul&gt;&lt;li&gt;该编译选项暂不支持printf、DumpTensor、DumpAccChkPoint、assert接口。&lt;/li&gt;&lt;/ul&gt; &lt;/div&gt;&lt;/div&gt; &lt;/td&gt; &lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;24.67%&quot;&gt;&lt;p&gt;ascendc_compile_definitions&lt;/p&gt; &lt;/td&gt; &lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;75.33%&quot;&gt;&lt;p&gt;添加编译宏。可以添加&lt;span&gt;Ascend C提供的编译宏和开发者自定义的编译宏。语法格式如下：&lt;/p&gt; &lt;pre class=&quot;screen&quot;&gt;ascendc_compile_definitions(&lt;target_name&gt; [PRIVATE] [&lt;xxx&gt;...]) &lt;/pre&gt; &lt;p&gt;&lt;span&gt;Ascend C提供的编译宏介绍如下：&lt;/p&gt; &lt;ul&gt;&lt;li&gt;HAVE_WORKSPACE用于表示kernel入口是否包含workspace入参。默认情况下为不包含；增加该编译宏后，表示包含，此时框架会获取kernel入参的倒数第一个参数（未配置&lt;a href=&quot;#li6933155615394&quot;&gt;HAVE_TILING），或倒数第二个参数（配置HAVE_TILING），自动在kernel侧设置系统workspace，开发者在kernel侧入参处获取的workspace为偏移了系统workspace后的用户workspace。当开发者使用了Matmul Kernel侧接口等需要系统workspace的高阶API时，建议开启此参数，入参排布、系统workspace的设置逻辑与工程化算子开发保持一致，可减少算子实现在不同开发方式间切换带来的修改成本。需要注意的是，host侧开发者仍需要自行申请workspace的空间，系统workspace大小可以通过PlatformAscendCManager的GetLibApiWorkSpaceSize接口获取。HAVE_WORKSPACE的设置样例如下：&lt;pre class=&quot;screen&quot;&gt;ascendc_compile_definitions(ascendc_kernels_\${RUN_MODE} PRIVATE HAVE_WORKSPACE )&lt;/pre&gt; &lt;/li&gt;&lt;li&gt;HAVE_TILING用于表示kernel入口是否含有tiling入参。在配置了HAVE_WORKSPACE之后，此编译宏才会生效。默认情况下为不包含，开关关闭；增加该编译宏后，表示包含，此时框架会将kernel入参的最后一个参数当做tiling，将倒数第二个参数当做workspace。框架不会对此tiling入参做任何处理，仅通过该入参来判断workspace参数的位置，使用此编译宏可以和工程化算子开发保持入参一致，减少算子实现在不同开发方式间切换带来的修改成本。设置样例如下：&lt;pre class=&quot;screen&quot;&gt;ascendc_compile_definitions(ascendc_kernels_\${RUN_MODE} PRIVATE HAVE_WORKSPACE HAVE_TILING )&lt;/pre&gt; &lt;/li&gt;&lt;/ul&gt; &lt;/td&gt; &lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;24.67%&quot;&gt;&lt;p&gt;ascendc_compile_options&lt;/p&gt; &lt;/td&gt; &lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;75.33%&quot;&gt;&lt;p&gt;添加编译选项。可以添加相应的编译选项用于host侧与device侧的编译过程。语法格式如下：&lt;/p&gt; &lt;pre class=&quot;screen&quot;&gt;ascendc_compile_options(&lt;target_name&gt; PRIVATE [&lt;xxx&gt;...] )&lt;/pre&gt; &lt;p&gt;默认情况下，指定的编译选项都将传递给device侧编译器进行编译。若想传递编译选项给host侧编译器，则需要使用“-forward-options-to-host-compiler”编译选项，该选项后的编译选项将传递给host侧编译器，示例如下：&lt;/p&gt; &lt;pre class=&quot;screen&quot;&gt;ascendc_compile_options(&lt;target_name&gt; PRIVATE -g -forward-options-to-host-compiler -gdwarf-4 )&lt;/pre&gt; &lt;p&gt;如上代码所示，在编译时，“-g”编译选项传递给device侧编译器，“-gdwarf-4”编译选项传递给host侧编译器。&lt;/p&gt; &lt;p&gt;备注：host侧编译选项只支持g++与clang编译器共同支持的编译选项。&lt;/p&gt; &lt;/td&gt; &lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;24.67%&quot;&gt;&lt;p&gt;ascendc_include_directories&lt;/p&gt; &lt;/td&gt; &lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;75.33%&quot;&gt;&lt;p&gt;添加开发者自定义的头文件搜索路径。语法格式如下：&lt;/p&gt; &lt;pre class=&quot;screen&quot;&gt;ascendc_include_directories(&lt;target_name&gt; [PRIVATE] [&lt;xxx&gt;...]) &lt;/pre&gt; &lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt;</p><p>简化的编译流程图如下图所示：将算子核函数源文件编译生成kernel侧的库文件（*.so或*.a库文件）；工程框架自动生成核函数调用接口声明头文件；编译main.cpp（算子调用应用程序）时依赖上述头文件，将编译应用程序生成的目标文件和kernel侧的库文件进行链接，生成最终的可执行文件。</p><p><strong>图 3</strong> 编译简化流程图<br><img src="`+i+`" alt="" title="编译简化流程图"></p><p>编译安装结束后在CMAKE_INSTALL_PREFIX目录下生成的编译产物示例如下；最终的可执行文件会生成在cmake命令的执行目录下。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>out</span></span>
<span class="line"><span>├── lib </span></span>
<span class="line"><span>│   ├── libkernels1.a</span></span>
<span class="line"><span>│   ├── libkernels2.so</span></span>
<span class="line"><span>├── include</span></span>
<span class="line"><span>│   ├── kernels1</span></span>
<span class="line"><span>│           ├── aclrtlaunch_matmul_custom.h</span></span>
<span class="line"><span>│           ├── aclrtlaunch_add_custom.h</span></span>
<span class="line"><span>│   ├── kernels2</span></span>
<span class="line"><span>│           ├── aclrtlaunch_xxx.h</span></span>
<span class="line"><span>│           ├── ...</span></span></code></pre></div><p>对于lib目录下生成的库文件可通过msobjdump工具进一步解析得到kernel信息，具体操作参见<a href="./msobjdump工具.html">msobjdump工具</a>。</p><h2 id="输入数据和真值数据生成以及验证脚本文件" tabindex="-1">输入数据和真值数据生成以及验证脚本文件 <a class="header-anchor" href="#输入数据和真值数据生成以及验证脚本文件" aria-label="Permalink to &quot;输入数据和真值数据生成以及验证脚本文件&quot;">​</a></h2><p>以固定shape的add_custom算子为例，输入数据和真值数据生成的脚本样例如下：根据算子的输入输出编写脚本，生成输入数据和真值数据。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#!/usr/bin/python3</span></span>
<span class="line"><span># -*- coding:utf-8 -*-</span></span>
<span class="line"><span>import numpy as np</span></span>
<span class="line"><span></span></span>
<span class="line"><span>def gen_golden_data_simple():</span></span>
<span class="line"><span>    input_x = np.random.uniform(1, 100, [8, 2048]).astype(np.float16)</span></span>
<span class="line"><span>    input_y = np.random.uniform(1, 100, [8, 2048]).astype(np.float16)</span></span>
<span class="line"><span>    golden = (input_x + input_y).astype(np.float16)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    input_x.tofile(&quot;./input/input_x.bin&quot;)</span></span>
<span class="line"><span>    input_y.tofile(&quot;./input/input_y.bin&quot;)</span></span>
<span class="line"><span>    golden.tofile(&quot;./output/golden.bin&quot;)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>if __name__ == &quot;__main__&quot;:</span></span>
<span class="line"><span>    gen_golden_data_simple()</span></span></code></pre></div><p>验证输出数据和真值数据是否一致的验证脚本样例如下：当前使用numpy接口计算了输出数据和真值数据的绝对误差和相对误差，误差在容忍偏差范围内，视为精度符合要求，输出&quot;test pass&quot;字样。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>import os</span></span>
<span class="line"><span>import sys</span></span>
<span class="line"><span>import numpy as np</span></span>
<span class="line"><span></span></span>
<span class="line"><span>loss = 1e-3 # 容忍偏差，一般fp16要求绝对误差和相对误差均不超过千分之一</span></span>
<span class="line"><span>minimum = 10e-10</span></span>
<span class="line"><span></span></span>
<span class="line"><span>def verify_result(real_result, golden):</span></span>
<span class="line"><span>    real_result = np.fromfile(real_result, dtype=np.float16) # 从bin文件读取实际运算结果</span></span>
<span class="line"><span>    golden = np.fromfile(golden, dtype=np.float16) # 从bin文件读取预期运算结果</span></span>
<span class="line"><span>    result = np.abs(real_result - golden) # 计算运算结果和预期结果偏差</span></span>
<span class="line"><span>    deno = np.maximum(np.abs(real_result), np.abs(golden)) # 获取最大值并组成新数组</span></span>
<span class="line"><span>    result_atol = np.less_equal(result, loss) # 计算绝对误差</span></span>
<span class="line"><span>    result_rtol = np.less_equal(result / np.add(deno, minimum), loss) # 计算相对误差</span></span>
<span class="line"><span>    if not result_rtol.all() and not result_atol.all():</span></span>
<span class="line"><span>        if np.sum(result_rtol == False) &gt; real_result.size * loss and np.sum(result_atol == False) &gt; real_result.size * loss:</span></span>
<span class="line"><span>            print(&quot;[ERROR] result error&quot;)</span></span>
<span class="line"><span>            return False</span></span>
<span class="line"><span>    print(&quot;test pass&quot;)</span></span>
<span class="line"><span>    return True</span></span>
<span class="line"><span></span></span>
<span class="line"><span>if __name__ == &#39;__main__&#39;:</span></span>
<span class="line"><span>    verify_result(sys.argv[1],sys.argv[2])</span></span></code></pre></div><h2 id="修改并执行一键式编译运行脚本" tabindex="-1">修改并执行一键式编译运行脚本 <a class="header-anchor" href="#修改并执行一键式编译运行脚本" aria-label="Permalink to &quot;修改并执行一键式编译运行脚本&quot;">​</a></h2><p>您可以基于样例工程中提供的一键式编译运行脚本进行快速编译，并在CPU侧和NPU侧执行Ascend C算子。一键式编译运行脚本主要完成以下功能：</p><p><strong>图 4</strong> 一键式编译运行脚本流程图<br><img src="`+o+`" alt="" title="一键式编译运行脚本流程图"></p><div class="note custom-block github-alert"><p class="custom-block-title">说明</p><p><strong>样例中提供的一键式编译运行脚本并不能适用于所有的算子运行验证场景，使用时请根据实际情况进行修改。</strong></p><ul><li>根据Ascend C算子的算法原理的不同，自行实现输入和真值数据的生成脚本。</li></ul></div><p>完成上述文件的编写后，可以执行一键式编译运行脚本，编译和运行应用程序。</p><p>脚本执行方式和脚本参数介绍如下：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>bash run.sh --run-mode=npu  --soc-version=&lt;soc_version&gt; --install-path=&lt;install_path&gt;  --build-type=Debug  --install-prefix=&lt;install-prefix&gt;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>bash run.sh -r npu  -v &lt;soc_version&gt; -i &lt;install_path&gt; -b Debug -p &lt;install-prefix&gt;</span></span></code></pre></div><p><strong>表 3</strong> 脚本参数介绍</p><p>&lt;table&gt;&lt;thead align=&quot;left&quot;&gt;&lt;tr&gt;&lt;th class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;16.14%&quot;&gt;&lt;p&gt;参数名&lt;/p&gt; &lt;/th&gt; &lt;th class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;11.690000000000001%&quot;&gt;&lt;p&gt;参数简写&lt;/p&gt; &lt;/th&gt; &lt;th class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;72.17%&quot;&gt;&lt;p&gt;参数介绍&lt;/p&gt; &lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt;&lt;tr&gt;&lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;16.14%&quot;&gt;&lt;p&gt;--run-mode&lt;/p&gt; &lt;/td&gt; &lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;11.690000000000001%&quot;&gt;&lt;p&gt;-r&lt;/p&gt; &lt;/td&gt; &lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;72.17%&quot;&gt;&lt;p&gt;表明算子以cpu模式或npu模式运行。&lt;/p&gt; &lt;p&gt;取值为cpu或npu。&lt;/p&gt; &lt;/td&gt; &lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;16.14%&quot;&gt;&lt;p&gt;--soc-version&lt;/p&gt; &lt;/td&gt; &lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;11.690000000000001%&quot;&gt;&lt;p&gt;-v&lt;/p&gt; &lt;/td&gt; &lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;72.17%&quot;&gt;&lt;p&gt;算子运行的AI处理器型号。&lt;/p&gt; &lt;/td&gt; &lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;16.14%&quot;&gt;&lt;p&gt;--install-path&lt;/p&gt; &lt;/td&gt; &lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;11.690000000000001%&quot;&gt;&lt;p&gt;-i&lt;/p&gt; &lt;/td&gt; &lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;72.17%&quot;&gt;&lt;p&gt;配置为CANN软件的安装路径，请根据实际安装路径进行修改。&lt;/p&gt; &lt;p&gt;默认值为$HOME/Ascend/ascend-toolkit/latest。&lt;/p&gt; &lt;/td&gt; &lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;16.14%&quot;&gt;&lt;p&gt;--build-type&lt;/p&gt; &lt;/td&gt; &lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;11.690000000000001%&quot;&gt;&lt;p&gt;-b&lt;/p&gt; &lt;/td&gt; &lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;72.17%&quot;&gt;&lt;p&gt;编译模式选项，可配置为：&lt;/p&gt; &lt;ul&gt;&lt;li&gt;Release，Release版本，不包含调试信息，编译最终发布的版本。&lt;/li&gt;&lt;li&gt;Debug，Debug版本，包含调试信息，便于开发者开发和调试。&lt;/li&gt;&lt;/ul&gt; &lt;p&gt;默认值为Debug。&lt;/p&gt; &lt;/td&gt; &lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;16.14%&quot;&gt;&lt;p&gt;--install-prefix&lt;/p&gt; &lt;/td&gt; &lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;11.690000000000001%&quot;&gt;&lt;p&gt;-p&lt;/p&gt; &lt;/td&gt; &lt;td class=&quot;cellrowborder&quot; valign=&quot;top&quot; width=&quot;72.17%&quot;&gt;&lt;p&gt;用于指定CMAKE执行install时，安装的路径前缀，执行install后编译产物（ascendc_library中指定的target以及对应的头文件）会安装在该路径下。默认路径为当前目录的out目录下。&lt;/p&gt; &lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt;</p><p>脚本执行完毕输出&quot;test pass&quot;字样表示算子精度符合要求。</p><h2 id="cpu侧验证核函数" tabindex="-1">CPU侧验证核函数 <a class="header-anchor" href="#cpu侧验证核函数" aria-label="Permalink to &quot;CPU侧验证核函数&quot;">​</a></h2><p>在非昇腾设备上，开发者可以利用CPU仿真环境先行进行算子开发和测试，并在准备就绪后，利用昇腾设备进行加速计算。在<a href="./../编译与运行/AI-Core-SIMD编译/算子编译简介.html">编译与运行</a>章节，我们已经介绍了算子Kernel程序NPU域的编译运行。相比于NPU域的算子运行逻辑，CPU域调试，实际上是通过标准的GCC编译器编译算子Kernel程序。此时算子Kernel程序链接CPU调测库，执行编译生成的可执行文件，可以完成算子CPU域的运行验证。CPU侧的运行程序，通过GDB通用调试工具进行单步调试，可以精准验证程序执行流程是否符合预期。</p><p><strong>图 5</strong> CPU域和NPU域的核函数运行逻辑对比<br><img src="`+c+`" alt="" title="CPU域和NPU域的核函数运行逻辑对比-36"></p><p>基于Kernel直调样例工程，通过ACLRT_LAUNCH_KERNEL接口调用核函数时，可实现CPU与NPU域的代码的统一，且该方式仅支持以下型号：</p><ul><li>Atlas A3 训练系列产品/Atlas A3 推理系列产品</li><li>Atlas A2 训练系列产品/Atlas A2 推理系列产品</li><li>Atlas 推理系列产品</li></ul><p>下面代码以add_custom算子为例，介绍算子核函数在CPU侧验证时，算子调用的应用程序如何编写（通过ACLRT_LAUNCH_KERNEL接口调用核函数的方式）。您在实现自己的应用程序时，需要关注由于算子核函数不同带来的修改，包括算子核函数名，入参出参的不同等，合理安排相应的内存分配、内存拷贝和文件读写等，相关API的调用方式直接复用即可。</p><ol><li><p>按需包含头文件。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &quot;data_utils.h&quot;</span></span>
<span class="line"><span>#include &quot;acl/acl.h&quot;</span></span>
<span class="line"><span>#include &quot;aclrtlaunch_add_custom.h&quot;</span></span></code></pre></div></li><li><p>应用程序框架编写。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int32_t main(int32_t argc, char* argv[])</span></span>
<span class="line"><span>{</span></span>
<span class="line"><span>    uint32_t numBlocks = 8;</span></span>
<span class="line"><span>    size_t inputByteSize = 8 * 2048 * sizeof(uint16_t);</span></span>
<span class="line"><span>    size_t outputByteSize = 8 * 2048 * sizeof(uint16_t);</span></span>
<span class="line"><span>    // 运行算子的调用程序</span></span>
<span class="line"><span>    return 0;</span></span>
<span class="line"><span>}</span></span></code></pre></div></li><li><p>运行验证。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    // 初始化</span></span>
<span class="line"><span>    CHECK_ACL(aclInit(nullptr));</span></span>
<span class="line"><span>    // 运行管理资源申请</span></span>
<span class="line"><span>    int32_t deviceId = 0;</span></span>
<span class="line"><span>    CHECK_ACL(aclrtSetDevice(deviceId));</span></span>
<span class="line"><span>    aclrtStream stream = nullptr;</span></span>
<span class="line"><span>    CHECK_ACL(aclrtCreateStream(&amp;stream));</span></span>
<span class="line"><span>    // 分配Host内存</span></span>
<span class="line"><span>    uint8_t *xHost, *yHost, *zHost;</span></span>
<span class="line"><span>    uint8_t *xDevice, *yDevice, *zDevice;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    CHECK_ACL(aclrtMallocHost((void**)(&amp;xHost), inputByteSize));</span></span>
<span class="line"><span>    CHECK_ACL(aclrtMallocHost((void**)(&amp;yHost), inputByteSize));</span></span>
<span class="line"><span>    CHECK_ACL(aclrtMallocHost((void**)(&amp;zHost), outputByteSize));</span></span>
<span class="line"><span>    // 分配Device内存</span></span>
<span class="line"><span>    CHECK_ACL(aclrtMalloc((void**)&amp;xDevice, inputByteSize, ACL_MEM_MALLOC_HUGE_FIRST));</span></span>
<span class="line"><span>    CHECK_ACL(aclrtMalloc((void**)&amp;yDevice, inputByteSize, ACL_MEM_MALLOC_HUGE_FIRST));</span></span>
<span class="line"><span>    CHECK_ACL(aclrtMalloc((void**)&amp;zDevice, outputByteSize, ACL_MEM_MALLOC_HUGE_FIRST));</span></span>
<span class="line"><span>    // Host内存初始化</span></span>
<span class="line"><span>    ReadFile(&quot;./input/input_x.bin&quot;, inputByteSize, xHost, inputByteSize);</span></span>
<span class="line"><span>    ReadFile(&quot;./input/input_y.bin&quot;, inputByteSize, yHost, inputByteSize);</span></span>
<span class="line"><span>    // 将数据从Host上拷贝到Device上</span></span>
<span class="line"><span>    CHECK_ACL(aclrtMemcpy(xDevice, inputByteSize, xHost, inputByteSize, ACL_MEMCPY_HOST_TO_DEVICE));</span></span>
<span class="line"><span>    CHECK_ACL(aclrtMemcpy(yDevice, inputByteSize, yHost, inputByteSize, ACL_MEMCPY_HOST_TO_DEVICE));</span></span>
<span class="line"><span>    // 用ACLRT_LAUNCH_KERNEL接口调用核函数完成指定的运算</span></span>
<span class="line"><span>    ACLRT_LAUNCH_KERNEL(add_custom)(numBlocks, stream, xDevice, yDevice, zDevice);</span></span>
<span class="line"><span>    CHECK_ACL(aclrtSynchronizeStream(stream));</span></span>
<span class="line"><span>    // 将Device上的运算结果拷贝回Host</span></span>
<span class="line"><span>    CHECK_ACL(aclrtMemcpy(zHost, outputByteSize, zDevice, outputByteSize, ACL_MEMCPY_DEVICE_TO_HOST));</span></span>
<span class="line"><span>    WriteFile(&quot;./output/output_z.bin&quot;, zHost, outputByteSize);</span></span>
<span class="line"><span>    // 释放申请的资源</span></span>
<span class="line"><span>    CHECK_ACL(aclrtFree(xDevice));</span></span>
<span class="line"><span>    CHECK_ACL(aclrtFree(yDevice));</span></span>
<span class="line"><span>    CHECK_ACL(aclrtFree(zDevice));</span></span>
<span class="line"><span>    CHECK_ACL(aclrtFreeHost(xHost));</span></span>
<span class="line"><span>    CHECK_ACL(aclrtFreeHost(yHost));</span></span>
<span class="line"><span>    CHECK_ACL(aclrtFreeHost(zHost));</span></span>
<span class="line"><span>    // 去初始化</span></span>
<span class="line"><span>    CHECK_ACL(aclrtDestroyStream(stream));</span></span>
<span class="line"><span>    CHECK_ACL(aclrtResetDevice(deviceId));</span></span>
<span class="line"><span>    CHECK_ACL(aclFinalize());</span></span></code></pre></div></li></ol><div class="note custom-block github-alert"><p class="custom-block-title">说明</p><p>为了实现CPU域与NPU域代码归一，仅对部分acl接口进行适配，开发者在使用CPU域调测功能时，仅支持使用如下acl接口：</p><ul><li>有实际功能接口，支持CPU域调用 <ul><li>aclDataTypeSize、aclFloat16ToFloat、aclFloatToFloat16。</li><li>aclrtMalloc、aclrtFree、aclrtMallocHost、aclrtFreeHost、aclrtMemset、aclrtMemsetAsync、aclrtMemcpy、aclrtMemcpyAsync、aclrtMemcpy2d、aclrtMemcpy2dAsync、aclrtCreateContext、aclrtDestroyContext。</li></ul></li><li>无实际功能接口，打桩实现。 <ul><li>Profiling数据采集 aclprofInit、aclprofSetConfig、aclprofStart、aclprofStop、aclprofFinalize。</li><li>系统配置 aclInit、aclFinalize、aclrtGetVersion。</li><li>运行时管理 aclrtSetDevice、aclrtResetDevice、aclrtCreateStream、aclrtCreateStreamWithConfig、aclrtDestroyStream、aclrtDestroyStreamForce、aclrtSynchronizeStream、aclrtCreateContext、aclrtDestroyContext。</li></ul></li></ul></div>`,50)])])}const b=a(r,[["render",u]]);export{v as __pageData,b as default};
